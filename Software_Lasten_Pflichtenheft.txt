╔══════════════════════════════════════════════════════════════════╗
║     LASTEN- UND PFLICHTENHEFT — SOFTWARE                        ║
║     Womo Energy Core v5.5.3                                      ║
║     Stand: Schaltkriterien-Redesign (nur harte AUS-Bedingungen, ║
║            Float-Erkennung, WR nur manuell EIN) + D+-Pegel-Fix  ║
║            (Active-HIGH) + Lagesensor Überkopf-Einbau/Fehler-   ║
║            toleranz + persistentes Manuell-AUS + Web-OTA +      ║
║            Lagesensor (MMA8452Q) + Renogy-NVS-Wechselrichter    ║
║            + mDNS (womo.local) + NTP-Zeitsync (v5.5.2)          ║
║            + NTP-Sync-Statusanzeige im Dashboard (v5.5.3)       ║
╚══════════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEIL 1 — LASTENHEFT (Was soll die Software leisten?)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

L-SW01 | DATENAKQUISITION BMS  [GEÄNDERT]                    [MUSS]
  Die Software liest folgende Daten aus dem JK-BMS (4S LFP 280Ah)
  über RS485 aus: SoC [%], Strom [A mit Vorzeichen + = LADEN /
  − = ENTLADEN (am Gerät verifiziert, Zweierkomplement int16)],
  Gesamtspannung [V], 3 Temperaturen (Tube/Kern, Sensor1/Rand,
  BMS/MOS), Restkapazität [Ah abgeleitet], Nennkapazität [Ah],
  MOSFET-Status (Laden/Entladen).
  Abfrage-Intervall: 2 Sekunden.
  Byte-Offsets und Skalierungsfaktoren nach Erstbetrieb mit echten
  Roh-Frames verifiziert (BMS_DEBUG_RAW=1 in bms.cpp) — siehe P-SW16.
  Alternative Anbindung (v5.1, optional, Compile-Zeit-Auswahl):
  CAN-Bus statt RS485 — identischer JSON-Output, siehe P-SW17.

L-SW02 | DATENAKQUISITION MPPT  [GEÄNDERT]                  [MUSS]
  Die Software liest folgende VE.Direct Text Protocol Felder des
  Victron MPPT 100/30: Batteriespannung (V), Ladestrom (I),
  PV-Leistung (PPV), Ladezustand (CS), Fehlercode (ERR),
  Off-Reason (OR), Tagesertrag (H20), Maximalleistung heute (H21).
  Senden erforderlich — kein reiner RX: Die Kerntemperatur des BMS
  (tempTube) wird via VE.Direct HEX-Protokoll an den MPPT gesendet,
  damit dieser temperaturkompensiertes Laden für den LFP-Akku
  durchführen kann. Text-Protokoll (Empfang) und HEX-Protokoll
  (Senden) laufen parallel auf UART2. Siehe P-SW04 und P-SW09.
  HINWEIS: VictronConnect TX-Modus konfigurieren (→ "VE.Direct").

L-SW03 | SCHALTLOGIK D+-RELAIS (KÜHLSCHRANK)  [GEÄNDERT v5.5][MUSS]
  EIN  wenn: SoC >= 95% (socDPlusOn)
             UND (PV(MA) >= 100W (pvDPlusMinW) ODER MPPT-Ladezustand
                  Float — im Float ist der Akku voll, Überschuss da)
             UND kein Landstrom
             UND BMS-Daten gültig UND nicht veraltet (> 60s)
             UND MPPT stabil (nach Recovery-Debounce)
  HALT wenn: bereits EIN, keine AUS-Bedingung erfüllt (Hysterese-Band)
  AUS  wenn (ausnahmslos HART — sofort, ohne Debounce):
             Landstrom erkannt
             ODER BMS-Daten ungültig oder veraltet (> 60s kein Frame)
             ODER SoC < 80% (socDPlusOff)
  v5.5: KEIN weiches PV-basiertes AUS mehr (socDPlusHigh und
    pvThresholdOff entfallen). Einmal eingeschaltet, läuft D+ bis
    eine harte Bedingung greift — der Kühlschrank zieht also abends/
    nachts planmäßig den Akku von 95% bis 80% (bewusste Entscheidung).
  v5.5: MPPT-Ausfall ist KEINE AUS-Bedingung mehr. Ein VE.Direct-
    Glitch bei vollem Akku schaltet nichts mehr ab; ungültige MPPT-
    Daten blockieren nur die EIN-Seite (PV/Float nicht bewertbar).
  v5.5: D+-Mindestlaufzeit entfallen — sie hemmte ausschließlich
    weiche AUS-Gründe, die es nicht mehr gibt.
  Hysterese: 15% SoC (EIN 95 / hart-AUS 80).
  Flatter-Schutz: EIN-Transition erst nach 10 stabilen Zyklen (20s);
    harte AUS-Gründe schalten sofort (B-11-Express-Route).
  PV-Glättung: gleitender Mittelwert über 30 Sekunden (15 × 2s).
  MPPT-Recovery-Debounce: 5 aufeinanderfolgende gültige Frames (≈ 10s)
    erforderlich, bevor MPPT nach Timeout wieder als "gesund" gilt.

L-SW04 | SCHALTLOGIK GEL-LADER (STARTERBATTERIE) [GEÄNDERT v5.5][MUSS]
  EIN  wenn: SoC >= 95% (socGelOn)
             UND (PV(MA) >= 20W (pvGelMinW) ODER MPPT-Ladezustand Float)
             UND kein Landstrom
             UND BMS-Daten gültig UND nicht veraltet (> 60s)
             UND MPPT stabil (nach Recovery-Debounce)
  HALT wenn: bereits EIN, keine AUS-Bedingung erfüllt
  AUS  wenn (ausnahmslos HART — sofort, ohne Debounce):
             Landstrom erkannt (NEU v5.5)
             ODER BMS-Daten ungültig oder veraltet (> 60s kein Frame)
             ODER SoC < 80% (socGelOff, eigener Parameter NEU v5.5)
  v5.5: Struktur identisch zu L-SW03 — kein weiches PV-AUS mehr
    (socGelHigh entfällt), MPPT-Ausfall keine AUS-Bedingung mehr.
  v5.5: Defaults geändert: socGelOn 82→95%, pvGelMinW 60→20W.
  Gleichzeitiger Betrieb mit D+-Relais möglich und gewollt.

L-SW05 | SCHALTLOGIK WECHSELRICHTER REMOTE [GEÄNDERT v5.5] [SOLLTE]
  KEIN Auto-EIN (v5.5): Einschalten ausschließlich manuell über das
  Dashboard (L-SW23) — ohne Deadman-Timer. socWROn entfällt.
  Die Automatik darf den WR nur noch ABschalten; diese Bedingungen
  gelten AUCH während Manuell-EIN und beenden ihn (Sicherheitsnetz,
  da der Timer entfällt):
  AUS  wenn: (hart)  BMS ungültig/veraltet (> 60s kein Frame)
             (weich) SoC < socWROff (Default 80%, debounced 10 Zyklen)
  Keine PV-Bedingung, kein MPPT-Check, kein Landstrom-Check.
  v5.3: Landstrom-Bedingung entfernt. Hardwarewechsel von Edecoa
  (ohne NVS) auf Renogy 12V/2000W MIT eingebauter Netzvorrang-
  schaltung (NVS) — das Gerät übernimmt die AC-seitige Umschaltung
  Landstrom/Inverter selbst, ein hartes Abschalten der Fernsteuerung
  bei Landstrom ist bei diesem Gerät funktional nicht nötig.

L-SW06 | LANDSTROM-ERKENNUNG                               [MUSS]
  Hardware-Signal (Spannungsteiler 2k/1,5k an 5V-Rail) wird als
  GPIO-Pin eingelesen. HIGH = Landstrom vorhanden.
  Bei Landstrom werden D+-Relais und Gel-Lader (v5.5) sofort
  deaktiviert. Die
  WR-Remote-Logik (L-SW05) ist seit v5.3 unabhängig von Landstrom
  (Renogy-NVS übernimmt die AC-Umschaltung selbst).

L-SW07 | FAIL-SAFE / WATCHDOG                              [MUSS]
  Zweistufiges Watchdog-Konzept:
   (a) Hardware-Watchdog (esp_task_wdt, 4 Sekunden) überwacht NUR die
       Hauptschleife loop(). Bei Loop-Hang → sofortiger harter Reset
       (Silizium-Reset, ohne SD-Rettung).
   (b) Software-Watchdog (watchdog.cpp) überwacht BMS, MPPT, Logger.
       Bei Modul-Hang: SD-Notsicherung, dann SOFORTIGER Reboot.
       Anti-Bootloop: 15 Minuten degradierter Betrieb, dann erneuter
       Versuch. Reboot-Grund in RTC-Memory (übersteht ESP.restart()).
  Alle Relais und MOSFETs fallen bei Stromlosigkeit in sicheren
  Aus-Zustand (Fail-Safe durch Active-LOW/HIGH der Hardware).

L-SW08 | RAM-RINGPUFFER                                    [MUSS]
  Alle 2 Sekunden wird ein Dateneintrag im PSRAM-Ringpuffer gespeichert.
  Puffergröße: 86.400 Einträge = 48 Stunden in voller 2s-Auflösung.
  Speicher: 86.400 × 32 Byte = 2,76 MB PSRAM (von 8 MB).
  Interner SRAM bleibt vollständig für Webserver und Stack frei.
  Bei Stromausfall gehen Pufferdaten verloren (flüchtig).

L-SW09 | SD-LOGGING                                        [MUSS]
  Aus den 2s-Rohwerten wird je MINUTE ein arithmetischer Mittelwert
  gebildet (eine CSV-Zeile pro Minute). Minutenzeilen werden gepuffert
  und alle logIntervalMs (Default 15 Minuten) als Batch geschrieben.
  Dateiformat: CSV, eine Datei pro UTC-Kalendertag (/log_NNNNN.csv,
  NNNNN = Tage seit 1970 in UTC).
  Inhalt: Timestamp (UTC-Epoch, siehe L-SW19), Spannung, Strom,
  SoC, 4 Temperaturen, Restkapazität, PV-Leistung, PV-Spannung,
  PV-Strom, MPPT-Status, Fehler, Landstrom/Aktoren-Flags.
  Alle SD-Zugriffe aus allen Tasks über g_sdMutex synchronisiert.

L-SW10 | WEBINTERFACE — ZUGANG  [GEÄNDERT v5.5.2]          [MUSS]
  ESP32-S3 arbeitet als WLAN Access Point.
  SSID: GODMODULE | Passwort: BM-hy678 | Kanal: 6 | Max. 3 Clients.
  Erreichbar unter http://192.168.4.1 (AP-Standard-IP).
  mDNS (v5.5.2): zusätzlich per Namen unter http://womo.local
  erreichbar — sowohl im AP-Modus als auch (nach Heimnetz-Verbindung)
  über die Heimnetz-IP. Der Dienst wird beim Boot angekündigt und bei
  STA-Verbindung erneut propagiert (Reannounce im ws_task, gleiche
  Kontext-Regel wie die WLAN-Rekonfiguration). Hostname: MDNS_HOSTNAME
  in config.h ("womo"). HTTP-Dienst auf WEBSERVER_PORT (80).
  Komplett offline-fähig — kein Internet erforderlich.
  Dashboard wird aus LittleFS (/index.html) geliefert.
  Heim-WLAN (STA, Dualmodus seit v5.1): parallel zum AP kann sich
  das Modul in ein Heimnetz einbuchen (Dashboard dann auch über die
  Heimnetz-IP erreichbar). APSTA teilt EIN Radio — bei STA-
  Verbindung wandert der AP zwingend auf den Router-Kanal.
  Multi-SSID (v5.5.1): bis zu 3 Netze konfigurierbar (NVS "wifi",
  ssid1/pass1..ssid3/pass3; Migration hebt den alten Einzel-Key
  beim ersten Boot nach Slot 1). Bei >1 konfiguriertem Netz wählt
  ein ASYNCHRONER Scan das stärkste bekannte Netz (Auswertung im
  ws_task, vollständig non-blocking, AP bleibt aktiv); solange
  getrennt, Rescan alle 60s (Heimkehr/Roaming). Bei genau einem
  Netz Direktverbindung mit Arduino-AutoReconnect (Verhalten wie
  bis v5.5). Bewusst KEINE Arduino-WiFiMulti-Klasse: deren
  blockierendes run() verträgt sich nicht mit dem AP+STA-Dualmodus
  und der bestehenden Reconnect-Strategie.
  Alle Slots leer = reiner AP-Betrieb.
  Passwörter werden über die API nie zurückgegeben.

L-SW11 | WEBINTERFACE — LIVE-DASHBOARD                     [MUSS]
  Aktualisierung alle 2 Sekunden via WebSocket (Pfad /ws, Port 80).
  Vier Tabs: Live | Verlauf | System | Einstellungen.
  Anzeige: SoC-Anzeige (SVG-Bogen mit Farbwechsel), alle bekannten
  BMS-Werte, alle bekannten MPPT-Werte, Aktoren-Status.
  Kopf-Uhr zeigt echte lokale Uhrzeit (Zeitzone wählbar, Sommerzeit
  automatisch — siehe L-SW19).
  Responsives Layout für Android-Handy und Windows-PC.

L-SW12 | WEBINTERFACE — LIVE-GRAPH  [GEÄNDERT]            [MUSS]
  Chart auf dem Live-Tab, darstellbare Metrik wählbar:
  SoC, Batterie-Spannung, Strom, PV-Leistung, PV-Spannung, Rest-Ah.
  Zeitfenster wählbar: 30min / 2h / 6h / 24h / 48h.
  Beim ersten Verbinden / bei Auswahl: Vorladung aus RAM-Puffer via
  REST-API. Große Fenster werden serverseitig dezimiert geladen
  (/api/buffer mit step-Parameter). Zeitachse: echte HH:MM (lokale
  Zeitzone) bei synchronisierter Zeit, sonst relativ (-30m … jetzt).
  Rendering: Pure Canvas JavaScript (offline, keine externen Libs),
  mit Gradient-Fill unter der Linie.

L-SW13 | WEBINTERFACE — HISTORISCHE DATEN                 [MUSS]
  Eigener Tab "Verlauf": Auswahl der CSV-Datei von SD-Karte,
  Laden und Anzeige als Graph. Metriken: SoC, Spannung, Strom,
  PV-Leistung. Bis zu 2.000 Datenpunkte pro Anfrage.

L-SW14 | WEBINTERFACE — EINSTELLUNGEN  [GEÄNDERT]          [MUSS]
  Alle variablen Parameter über Webformular änderbar und sofort
  persistent in NVS gespeichert. Fabrik-Reset-Funktion vorhanden.
  Änderungen wirken ohne Neustart sofort auf die Schaltlogik.
  Plausibilitätsgrenzen werden geprüft (Wertebereichsgrenzen);
  keine erzwungene ON > OFF Cross-Validierung mehr.
  WR-Parameter (socWROff) ist unabhängig von D+ konfigurierbar;
  socWROn entfällt seit v5.5 (WR-EIN nur manuell, siehe L-SW05).

L-SW15 | PARAMETER-PERSISTENZ (NVS)                       [MUSS]
  Alle Schwellwerte und Betriebsparameter werden im NVS-Flash
  des ESP32-S3 gespeichert (Namespace "womo_cfg"). Beim ersten Start
  werden Default-Werte aus config.h geschrieben. Fabrik-Reset nur
  über Webinterface. Die zuletzt bekannte Uhrzeit wird zusätzlich im
  NVS-Namespace "clock" gehalten (siehe L-SW19).

L-SW16 | AUTARKIE                                          [MUSS]
  Das System läuft 100% ohne WLAN/Internet/Cloud. Keine externen
  Dienste, keine Cloud-Abhängigkeiten. Dauerbetrieb im Fahrzeug
  ohne Netzwerkverbindung nach außen.

L-SW17 | WR-STATUS (RENOGY NVS, LED-EINGÄNGE)   [GEÄNDERT v5.3] [KANN]
  Hardwarewechsel: Edecoa (RJ12-Protokoll-Sniffer, nie über Stub
  hinaus implementiert) ersetzt durch Renogy 12V/2000W mit NVS.
  Kein Datenprotokoll am Fernbedienungsport vorhanden — nur zwei
  rohe LED-Spannungspegel (Power/grün, Alarm/rot) + Schalterkontakt.
  Implementiert: 2 digitale Status-Eingänge (debounced), GPIO 4/5
  über Spannungsteiler. AC-Ausgangsleistung/-Spannung NICHT
  verfügbar (Hardware-Limitierung, kein Stub mehr — endgültig).
  Pinbelegung/Spannungsteiler-Dimensionierung: Multimeter-Messung
  am realen Gerät noch ausstehend (s. Anschluss_Anleitung MODUL 8b).

L-SW18 | DELPHI FIREMONKEY APP (PHASE 2)                  [SOLLTE]
  Eigene Android/Windows-App in Delphi 11 Alexandria (FireMonkey).
  Kommunikation über BLE 5.0 oder dieselbe REST/WebSocket-API.
  Implementierung nach Fertigstellung der ESP32-Firmware.
  Anforderungen: Live-Daten, historische Charts, Read-Only + Reset.

L-SW19 | ZEITBASIS (DS3231-RTC FÜHREND)  [GEÄNDERT v5.5.3]  [MUSS]
  Speicher- und Rechenformat ist IMMER UTC. Anzeige und lokale
  CSV-Spalte nutzen eine wählbare Zeitzone mit automatischer
  Sommer-/Winterzeit (DST) — Default Europe/Berlin (MEZ/MESZ).
  Die Zeitzone wird als vollwertiger POSIX-TZ-String gehalten
  (newlib setenv("TZ")+tzset()/localtime_r); zur Laufzeit über
  /api/tz änderbar und in NVS-Namespace "clock"/"tz" persistiert.
  Das System besitzt eine DS3231-Hardware-RTC (I2C, GPIO 1/2),
  seit v5.1 bestückt und produktiv. Sie ist die führende Zeitquelle
  und übersteht Stromausfälle eigenständig (Pufferbatterie) — eine
  Browserverbindung ist für korrekte Zeit nicht mehr erforderlich.
  Fallback-Kette: DS3231 → NVS (zuletzt bekannte Zeit) → Firmware-
  Build-Zeit (nur beim allerersten Boot ohne gestellte RTC, OSF-Flag).
  Browser-Sync (POST /api/time) bleibt als bequeme Stellmöglichkeit
  und Drift-Korrektur erhalten und schreibt zusätzlich die DS3231-
  Hardware-Uhr. Tägliche Resync gegen DS3231 bei UTC-Tageswechsel
  inkl. Drift-Messung (Soll- vs. Ist-Zeit in Sekunden).
  NTP (v5.5.2): Sobald eine STA-Verbindung (Heimnetz mit Internet)
  besteht, stellt sich die Uhr automatisch per SNTP — der Browser-
  Sync ist dafür nicht mehr nötig, bleibt aber der Weg im AP-Only-
  Betrieb (WoMo unterwegs, kein Internet). Bewusst rohe esp_sntp-API
  im POLL-Modus, KEIN configTime() — letzteres würde den POSIX-TZ
  überschreiben. SNTP stellt die System-UTC (settimeofday); ein
  Callback meldet den Sync, das Übertragen in die DS3231-Zeitbasis
  läuft über clock_set_epoch() im ws_task (kein I2C im SNTP-Task).
  Damit gilt für NTP dieselbe 5-s-Hysterese wie für den Browser-Sync;
  der POLL-Modus resynct danach selbstständig periodisch. NTP-Server:
  NTP_SERVER in config.h (globaler Pool "pool.ntp.org", auslandstauglich).
  Anzeige (v5.5.3): Der Zeitpunkt des letzten erfolgreichen NTP-Syncs
  wird als UTC-Epoch im Live-JSON (net.ntp, 0 = nie) geführt und im
  Dashboard-Zeitzone-Tab lokalzeit-formatiert angezeigt ("noch nicht
  synchronisiert", solange kein Sync erfolgt ist).
  Alle Log-Zeitstempel (RAM und CSV) und SD-Dateinamen nutzen
  UTC-Epoch intern. Nur Dashboard-Anzeige und die lokale CSV-Spalte
  (ts_mez) werden über den TZ-Offset (DST-korrekt) umgerechnet.
  Zeitbasis intern: gekapselter, Spinlock-geschützter esp_timer-Zähler
  (64-Bit µs) zwischen den seltenen I2C-Lesevorgängen — vermeidet
  I2C-Bus-Zugriff bei jedem clock_now()-Aufruf.
  clock_set_epoch() gibt bool zurück (false = Epoch unplausibel < 2024)
  und stellt zusätzlich die DS3231-Hardware-Uhr.
  RTC-Health (10s gecacht, kein I2C-Zugriff je Webanfrage): JSON
  {present, valid, drift, temp} über clock_rtc_json() — present =
  DS3231 antwortet aktuell auf I2C, valid = Zeit plausibel gesetzt
  (kein OSF), drift = Sekunden-Abweichung beim letzten Resync,
  temp = Chip-interne Temperatur (DS3231 kann KEINE Batteriespannung
  über I2C melden — das ist eine Hardware-Grenze des Bausteins).
  Hardware-Hinweis: I2C-Initialisierung erfordert delay(100) nach
  Wire.begin() + 3 Versuche Retry-Loop in clock_init() (DS3231 lieferte
  beim Kaltstart sonst sporadisch I2C-Timeout/Error 263).
  32K- und SQW-Pins der DS3231 bleiben unbeschaltet (Open-Drain,
  unkritisch) — keine Alarm-Interrupt-Funktion in v5.1 genutzt.

L-SW20 | STATUS-ANZEIGE ON-BOARD RGB-LED  [NEU v5.1]       [SOLLTE]
  Die On-board-RGB-LED (WS2812, GPIO 48) zeigt den Zustand aller
  relevanten Sensoren/Aktoren gleichzeitig und gleichberechtigt an —
  kein einzelner Prioritätszustand verdrängt einen anderen (Ausnahme:
  Heap-Warnung, siehe unten). Jeder Kanal bekommt eine feste Position
  in einem wiederkehrenden zeitlichen Muster ("Rundlauf"); ob ein
  Kanal aktiv ist, erkennt man an Position + Farbe, nicht an
  Verdrängung anderer Zustände.
  Kanäle (fixe Reihenfolge): BMS-Fehler, MPPT-Fehler, SoC-Gauge
  (immer sichtbar, farbcodiert nach Schwellwerten), Landstrom,
  D+ Kühlschrank, Gel-Lader, WR-Remote.
  Sonderfall Heap-Warnung: bei kritisch wenig freiem SRAM wird die
  LED unmittelbar und ohne auf den Rundlauf zu warten magenta
  geschaltet — Selbstschutz-Signal, keine Wartezeit erlaubt.
  Start-/Bootzustand: weiß, bis die Schaltlogik erstmals ausgewertet
  wurde. Implementierungsdetails siehe P-SW18.

L-SW21 | LAGESENSOR / ELEKTRONISCHE WASSERWAAGE  [NEU v5.4] [KANN]
  Optionales Modul: misst die Fahrzeugneigung (Roll/Pitch) im
  Stillstand über einen 3-Achs-Beschleunigungssensor (MMA8452Q) am
  gemeinsamen I2C-Bus mit der DS3231-RTC (GPIO 1/2). Kein eigener
  GPIO nötig — Versorgung 3,3V direkt, kein Pegelwandler.
  Aus der gemessenen Neigung wird je Rad die nötige Keilhöhe
  berechnet: das höchste Rad ist Referenz (Keil 0), die übrigen drei
  werden rechnerisch auf dessen Höhe angehoben — das tiefste Rad
  bekommt den größten Keil.
  Konfigurierbar: Spurweite (800–3000mm), Radstand (1500–8000mm),
  Einbaudrehung des Sensors (0/90/180/270°), Achsen-Invertierung,
  Überkopf-Einbau (flipZ, NEU v5.5 — für kopfüber montierte
  Platine, s. P-SW19), Modul aktiv/inaktiv.
  Kalibrierung: aktuelle Lage als "eben" (Null-Offset) speicherbar
  und zurücksetzbar — gleicht eine nicht exakt waagerechte Sensor-
  montage aus.
  Dashboard-Tab "Lage": Libellen-Anzeige (SVG-Blase wandert zur
  hohen Seite), Top-Down-Fahrzeugansicht mit Keilhöhe je Rad in cm,
  Kalibrierungs-Schaltflächen, Konfigurationsformular.
  Reine Komfortfunktion ohne Einfluss auf Schaltlogik oder
  Sicherheitsfunktionen — Ausfall des Sensors hat keine Auswirkung
  auf den restlichen Betrieb (present=false wird im Dashboard
  angezeigt, sonst keine Reaktion).
  Implementierungsdetails siehe P-SW19.

L-SW22 | WEB-OTA (FIRMWARE- & DASHBOARD-UPDATE)  [NEU v5.4.1] [SOLLTE]
  Firmware- und Dashboard-Updates müssen ohne PC/USB direkt aus dem
  Web-Dashboard einspielbar sein — Voraussetzung für den mobilen
  Betrieb ohne angeschlossenen Rechner (Kernziel des Projekts).
  Zwei Update-Arten über den System-Tab:
   (a) Firmware  — die Anwendung selbst (firmware.bin).
   (b) Dashboard — das LittleFS-Image mit index.html (littlefs.bin),
       damit auch reine Oberflächen-Änderungen ohne USB möglich sind.
  Ausfallsicherheit: Ein fehlgeschlagenes Firmware-Update darf das
  System nicht unbrauchbar machen — die zuvor laufende Anwendung
  bleibt in ihrer eigenen Partition bootfähig (Dual-App-Layout).
  Nach erfolgreichem Update sichert das Gerät den RAM-Ringpuffer auf
  SD und startet selbsttätig neu; das Dashboard verbindet sich nach
  dem Neustart automatisch wieder.
  Einmalige Voraussetzung: Der Wechsel vom bisherigen Single-App-
  Partitionslayout auf das Dual-App-Layout erfordert genau einen
  letzten USB-Flash mit vorherigem Flash-Erase (löscht alle NVS-
  Namespaces — siehe P-SW20 / Anschluss-Anleitung).
  Implementierungsdetails siehe P-SW20.


L-SW23 | MANUELLER AKTOR-OVERRIDE  [NEU v5.4, GEÄNDERT v5.5] [SOLLTE]
  Jeder Aktor (D+, Gel-Lader, WR-Remote) ist im Dashboard (System-
  Tab) per Segmentbuttons Auto/Ein/Aus manuell übersteuerbar
  (POST /api/manual, {"actuator","mode"}).
  Manuell EIN (D+/Gel): übersteuert JEDE Automatik-Bedingung inkl.
    Hart-Interlocks (bewusste Design-Entscheidung v5.4). Sicherheits-
    netz: Deadman-Timeout (manualTimeoutMin, Default 30 min, Refresh
    bei jedem Befehl) — bei Ablauf Rückfall in die Automatik. Nie
    persistent: Reboot = Auto.
  Manuell EIN (WR, v5.5): OHNE Deadman-Timer. Läuft bis zum nächsten
    Nutzerbefehl ODER bis eine Auto-AUS-Bedingung aus L-SW05 greift —
    diese beenden den Manual-Modus (Sicherheitsnetz ersetzt Timer).
    Nie persistent.
  Manuell AUS (alle Aktoren, v5.5): DAUERHAFT — kein Timer, kein
    automatischer Rückfall in die Automatik. NVS-persistent
    (Namespace "logic", Schlüssel manOff0..2): überlebt Reboot und
    Watchdog-Reset. AUS ist die sichere Richtung, Persistenz daher
    unbedenklich. Aufhebung nur durch expliziten Auto-/Ein-Befehl.
  Anzeige: Reason-String je Aktor ("MANUAL EIN (Auto in Ns)",
    "MANUAL AUS (dauerhaft)", …) + aktiver Segmentbutton.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEIL 2 — PFLICHTENHEFT (Wie ist die Software umgesetzt?)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

P-SW01 | PLATTFORM UND ENTWICKLUNGSUMGEBUNG  [GEÄNDERT v5.1]
  Hardware:    ESP32-S3 DevKitC-1 N16R8 (16MB Flash, 8MB Octal PSRAM)
  Framework:   Arduino (über PlatformIO + VS Code)
  Board-ID:    esp32-s3-devkitc-1
  Platform:    espressif32 @ ^6.9.0  ← GEPINNT (Arduino-Core 2.x,
               IDF 4.4). Arduino-Core 3.x / IDF 5 bricht die
               esp_task_wdt_init(uint32_t, bool)-Signatur von watchdog.cpp.
  KRITISCH (Inbetriebnahme-Erkenntnis): board_upload.flash_size UND
               board_build.flash_size MÜSSEN BEIDE auf 16MB gesetzt sein.
               Fehlt der upload-Key, meldet der Bootloader-Header 8MB,
               während partitions_16mb.csv auf 16MB ausgelegt ist →
               Bootloader verwirft die Partitionstabelle, Endlos-Reset
               ohne App-Ausgabe (nur ROM-Zeilen im Monitor). Zusätzlich
               board_build.arduino.memory_type=qio_opi für Octal-PSRAM
               erforderlich. Bei Partitionsänderung: pio run -t erase
               vor dem nächsten Upload.
  Partition:   Eigene partitions_16mb.csv — Dual-App OTA (v5.4.1):
               nvs 20kB | otadata 8kB | app0/ota_0 4MB |
               app1/ota_1 4MB | spiffs 8000kB (LittleFS) |
               coredump 128kB. Exakt 16MB, App-Partitionen 64k-
               aligned. (Bis v5.4: Single-App ohne OTA, nvs 20kB |
               app0 4MB factory | spiffs ~11,8MB | coredump 128kB —
               Migration siehe P-SW20.)
  Filesystem:  LittleFS (Partition-Label "spiffs", Web-Dateien: index.html)
               Eigener Build-Schritt nötig: pio run -t uploadfs
               (unabhängig vom Firmware-Upload).
  Build-Flags: -DBOARD_HAS_PSRAM -DARDUINO_USB_MODE=1
               -DARDUINO_USB_CDC_ON_BOOT=1
  Libraries:   ← ALLE MIT OWNER UND VERSION GEPINNT:
               esp32async/ESPAsyncWebServer @ ^3.7.0
               esp32async/AsyncTCP          @ ^3.3.2
               bblanchon/ArduinoJson        @ ^6.21.5
               FS, SD, LittleFS
               ESPAsyncTCP explizit ausgeschlossen (ESP8266-Fork).
  CAN-Build (optional, BMS_USE_CAN): build_flags = -D BMS_USE_CAN,
               build_src_filter = +<*> -<bms.cpp> (siehe P-SW17).

P-SW02 | PROJEKTSTRUKTUR (QUELLDATEIEN)  [GEÄNDERT v5.1]
  src/config.h          GPIO-Konstanten + DEFAULT_-Parameter +
                        LOGIC_-Tuning-Konstanten (MA, Recovery, Min-on)
  src/params.h/.cpp     NVS-Verwaltung (10 Parameter)
  src/bms.h/.cpp        JK-BMS RS485 Parser (ID-Tabelle, 16-Bit-CHK)
  src/bmscan.h/.cpp     JK-BMS CAN-Parser — Compile-Zeit-Alternative
                        zu bms.cpp, identischer JSON-Output (v5.1, neu)
  src/mppt.h/.cpp       VE.Direct Text-Parser + HEX-TX Temperatur
  src/inverter.h/.cpp   WR-Status Renogy NVS (2 LED-Eingänge, debounced)
  src/io.h/.cpp         GPIO-Init, Aktoren, Landstrom-Sensor,
                        RGB-LED-Rundlauf-Task (v5.1)
  src/logic.h/.cpp      Schaltlogik (Hysterese, MA, Recovery, Min-on),
                        setzt RGB-Kanalmaske (v5.1)
  src/logger.h/.cpp     PSRAM-Ringpuffer + SD-Logging + g_sdMutex
  src/clock.h/.cpp      Zeitdienst (DS3231-RTC führend, v5.1 produktiv)
  src/watchdog.h/.cpp   Software-Modulüberwachung BMS/MPPT/Logger
  src/http_server.h/.cpp HTTP-Server, WebSocket, REST-API (Streaming)
  src/ota.h/.cpp        Web-OTA: Firmware- & Dashboard-Upload per
                        Browser (Update.h, Dual-App), neu v5.4.1
  src/main.cpp          Setup, FreeRTOS-Tasks, HW-Watchdog
  data/index.html       Dashboard (LittleFS, Offline-fähig)
  partitions_16mb.csv   Partitionstabelle (Dual-App OTA, v5.4.1)
  platformio.ini        Build-Konfiguration (gepinnte Abhängigkeiten)
  Zentrale Versionskonstante FW_VERSION in config.h (v5.4.1) —
  speist Boot-Banner (main.cpp) und GET /api/ota; bei jedem Release
  nur dort + in den Datei-Kopfzeilen ändern.

P-SW03 | GPIO-BELEGUNG (FINAL, ESP32-S3 N16R8)  [GEÄNDERT v5.4]
  GPIO  1   → I2C SDA   DS3231 RTC (bestückt, produktiv seit v5.1) +
              optional Lagesensor MMA8452Q (geteilter Bus, v5.4)
  GPIO  2   → I2C SCL   DS3231 RTC (bestückt, produktiv seit v5.1) +
              optional Lagesensor MMA8452Q (geteilter Bus, v5.4)
  GPIO  4   → INPUT     WR-Status Power-LED (Renogy NVS, Spannungsteiler,
              Pinbelegung/Dimensionierung am Gerät noch zu verifizieren)
  GPIO  5   → INPUT     WR-Status Alarm-LED (Renogy NVS, Spannungsteiler,
              s.o.)
  GPIO  6–10→ frei (ADC1, für analoge Erweiterungen)
  GPIO 11   → SPI CS    SD-Karte
  GPIO 12   → SPI CLK   SD-Karte
  GPIO 13   → SPI MOSI  SD-Karte
  GPIO 14   → SPI MISO  SD-Karte
  GPIO 15   → frei       kein MAX485 mehr (JK-BMS direkt am GPS-Port)
  GPIO 16   → UART1 RX  JK-BMS direkt vom GPS-Port (TTL) / CAN RX (alt.)
  GPIO 17   → UART1 TX  JK-BMS direkt zum GPS-Port (TTL) / CAN TX (alt.)
  GPIO 18   → INPUT     Landstrom-Sensor (Spannungsteiler 2k/1,5k,
              KEIN Pull-up, HIGH = Landstrom)
  GPIO 21   → OUTPUT    D+-Relais Kühlschrank (Active-LOW)
  GPIO 38   → UART2 RX  VE.Direct MPPT Text-Protokoll (RX-only Pfad)
  GPIO 39   → OUTPUT    Starter-Lader MOSFET (Active-HIGH)
              HINWEIS: Standard-JTAG (MTCK). Nutzbar da USB-JTAG aktiv.
  GPIO 40   → OUTPUT    WR-Remote Optokoppler (Active-HIGH)
  GPIO 41   → OUTPUT    Status-LED (einfache LED, extern)
  GPIO 48   → OUTPUT    On-board RGB-LED WS2812 (Rundlauf-Encoding,
              GEÄNDERT v5.1 — siehe L-SW20/P-SW18)
  GPIO 42   → UART2 TX  VE.Direct MPPT HEX-Protokoll (TX, neu v5.0)
              HINWEIS: Standard-JTAG (MTMS). Nutzbar da USB-JTAG aktiv.
              3,3V Pegel — MPPT VE.Direct-Eingang ist 3,3V-kompatibel,
              kein Pegelwandler für TX-Richtung erforderlich.
  GESPERRT: 0,3,19,20,26-37,45,46 (Strapping/Flash/USB/PSRAM)
  RESERVIERT ANALOG: GPIO 6–10 (ADC1-sicher, für Erweiterungen)
  GPIO 16/17 bei CAN-Build (BMS_USE_CAN): statt der UART-TTL-Direkt-
    verbindung zum GPS-Port sitzt an denselben Pins ein CAN-
    Transceiver (z. B. SN65HVD230, 3,3V).

P-SW04 | SERIELLE SCHNITTSTELLEN  [GEÄNDERT]
  UART1 (BMS):  GPIO 16 RX / 17 TX, 115200 Baud, 8N1
                Direktverdrahtung zum JK-BMS-GPS-Port (UART-TTL, kein
                MAX485). GPIO 15 unbeschaltet (DE/RE-Toggle in
                bms.cpp bleibt No-Op).
                Polling: Query-Frame alle 2000ms; adaptiver Empfang
                (Abbruch bei 40ms Pause oder vollständigem Frame)
  UART2 (MPPT): GPIO 38 RX / GPIO 42 TX, 19200 Baud, 8N1
                Spannungsteiler RX: 1kΩ + 2kΩ (5V→3,3V)
                TX: kein Pegelwandler nötig (3,3V kompatibel)
                Text-Protokoll (RX): MPPT sendet automatisch alle ~1s
                HEX-Protokoll (TX): ESP32 sendet BMS-Temperatur an MPPT
                Beide Protokolle laufen auf demselben UART2.
  USB CDC:      Serieller Monitor, 115200 Baud

P-SW05 | FREERTOS TASK-ARCHITEKTUR
  Core 1 | Prio 4 | logic_task  (8kB Stack)
    → watchdog_kick(WDT_BMS), BMS poll (2s), Schaltlogik,
      logger_append()
  Core 1 | Prio 3 | mppt_task   (4kB Stack)
    → watchdog_kick(WDT_MPPT), VE.Direct lesen (100ms),
      HEX-TX Temperatur senden, inverter_poll()
  Core 0 | Prio 2 | logger_task (4kB Stack)
    → watchdog_kick(WDT_LOGGER) + logger_flush_sd(), alle 5s
  Core 0 | Prio 2 | ws_task     (4kB Stack)
    → webserver_broadcast() alle 2s an alle WS-Clients
  Core 0 | Prio 1 | level_task  (4kB Stack, optional)
    → MMA8452Q lesen (250ms = 4Hz), Neigung + Keilgeometrie
      berechnen, Ergebnis in Cache ablegen (kein I2C in HTTP-Pfad)
  Core 0 | Prio 1 | watchdog_task  (in watchdog.cpp)
    → überwacht Modul-Timer, hängt selbst am HW-WDT (Backstop)
  Main Loop:
    → esp_task_wdt_reset() (HW-WDT, 4s) + Heap-Überwachung (< 20kB)
    → ota_tick(): führt einen nach erfolgreichem Web-OTA geplanten
      Neustart aus (Ringpuffer-Sicherung + ESP.restart()) — bewusst
      im loop-Kontext, NICHT im AsyncTCP-Task (siehe P-SW20).

P-SW06 | MUTEX-KONZEPT (THREAD-SICHERHEIT)  [GEÄNDERT v5.4]
  g_bmsMutex   (bms.cpp)    → schützt BMSData g_bms
  g_mpptMutex  (mppt.cpp)   → schützt MPPTData g_mppt inkl. Timeout-Flag
  g_sdMutex    (logger.cpp) → schützt ALLE SD-Operationen im Projekt
  g_i2cMutex   (clock.h, deklariert) → koordiniert den GEMEINSAMEN
               I2C-Bus zwischen DS3231 (clock.cpp) und optionalem
               Lagesensor MMA8452Q (level.cpp) — beide Module greifen
               aus verschiedenen Tasks/Cores zu, ohne Mutex wäre ein
               I2C-Transaktions-Interleave möglich. (NEU v5.4)
  s_mutex      (logger.cpp) → schützt Ringpuffer + Minuten-Akkumulator
  s_reason_mux (logic.cpp)  → portMUX Spinlock für Reason-Strings
  logic.cpp liest g_bms/g_mppt mutex-geschützt in lokale Snapshots.
  Timeout bei allen xSemaphoreTake: 10–200ms je nach Priorität.

P-SW07 | DATENSTRUKTUREN  [GEÄNDERT]
  BMSData (bms.h):  Temperaturen [4], Spannung, Strom (+ = LADEN),
    Leistung, SoC, Kapazitäten (Nenn + abgeleitet), MOSFET-Status,
    Frame-/Fehlerzähler, lastUpdateMs (für Staleness-Check).
    Entfernt ggü. v4.x: Zellspannungen, Zyklen, Balancing,
    Alarm-/Schutz-Bitmasks, Schutzparameter.
  MPPTData (mppt.h): Live-Messwerte (V, I, PPV), Ladezustand (CS),
    Fehler (ERR), Off-Reason (OR), Tagesertrag (H20), Maximalleistung
    heute (H21), Frame-/Timeoutstatus, lastUpdateMs.
  IOState (io.h): Landstrom-Flag, Zustände aller 4 Aktoren.
  LogEntry (logger.h): 32 Byte, __attribute__((packed)), static_assert.
    Timestamp = UTC-Epoch intern (Anzeige in lokaler Zeitzone, DST).
  Params (params.h): 10 NVS-Parameter, global als g_params.
    Neu in v5.0: socDPlusHigh, socGelHigh.
    Entfernt: socGelOff (= socGelOn), pvWRThresholdOn, pvWRThresholdOff.
  Zeit (clock.cpp): DS3231-RTC führend (I2C), NVS-Fallback, Spinlock-
    geschützter esp_timer-Laufzeitzähler zwischen I2C-Lesevorgängen.
    10s gecachte Health-Werte {present,valid,drift,temp} für Webtask.
    Zeitzone: wählbarer POSIX-TZ-String (DST automatisch), Default
    Europe/Berlin; UTC intern, lokal nur Anzeige (siehe L-SW19).

P-SW08 | JK-BMS PROTOKOLL-IMPLEMENTIERUNG  [GEÄNDERT]
  Query-Frame (21 Byte, Checksum 0x0129):
    4E 57 00 13 00 00 00 00 06 03 00 00 00 00 00 00 68 00 00 01 29
  Antwort: SOF 4E 57, LEN (2 Byte), Daten..., 68, REC, CHK (2 Byte)
  ID→Länge-Tabelle: Bekannte Identifier mit fixer Payload-Länge
    tabelliert → kein Parser-Desync bei unbekannten Feldern.
  0x79 (Zellspannungen): Wird empfangen aber nicht gespeichert (v5.0).
  Temperaturen 0x80–0x82: 1°C-Schritte; Werte > 100 = negativ.
  Checksumme: 16-Bit (Summe & 0xFFFF, letzte 2 Byte).
  Framelänge adaptiv: Kandidaten LEN+2, LEN+4 und rx_len geprüft.
  Header-Resync: align_to_header() wirft Müll-Bytes vor 0x4E 0x57 weg.
  Strom-Kodierung: BMS_CURRENT_MODE/SCALE in config.h. + = LADEN.
  Staleness: millis() − lastUpdateMs < BMS_STALE_TIMEOUT_MS (60s).
  bms_to_json() liefert: valid, soc, v, i, pw, Temperaturen,
    rem, nom, mosfet-Status, frameCount, errorCount.
    Nicht mehr enthalten: Zellspannungen, Zyklen, Balancing, Alarmflags,
    Schutzparameter.
  Roh-Frame-Logging: dump_frame() bei CHK-Fehler; BMS_DEBUG_RAW=1
    für alle Frames. Byte-Offsets nach Erstbetrieb verifizieren!

P-SW09 | VE.DIRECT PROTOKOLL-IMPLEMENTIERUNG  [GEÄNDERT]
  Text-Protokoll (Empfang, unverändert):
    Format: ASCII Key\tValue\r\n, Block endet mit "Checksum\tByte\r\n"
    MPPT sendet automatisch alle ~1s — kein Request-Frame nötig.
    Parsing: Zeile für Zeile, Tab-Trennzeichen, Commit bei "Checksum".
    Timeout: nach MPPT_FRAME_TIMEOUT_MS (3000ms) →
      g_mppt.timeout = true (unter g_mpptMutex gesetzt).
  HEX-Protokoll (Senden, neu v5.0):
    Ziel: BMS-Kerntemperatur (tempTube) an MPPT senden, damit der
    MPPT temperaturkompensiertes Laden (LFP-optimiert) durchführt.
    Protokoll: Victron VE.Direct HEX (bidirektional, binär kodiert,
    ASCII-Hex-Darstellung über UART). Nur das für Temperatur-Senden
    nötige Subset wird implementiert — kein vollständiger HEX-Stack.
    Sendeintervall: alle 10s aus mppt_task heraus.
    Fehlerbehandlung: fehlgeschlagene HEX-Übertragungen werden
    ignoriert (Non-blocking; Text-Protokoll hat Priorität).
    UART2 TX: GPIO 42 (3,3V, kein Pegelwandler erforderlich).

P-SW10 | SCHALTLOGIK DETAIL (logic.cpp)  [GEÄNDERT v5.5]
  Hysterese: want = aktueller Aktor-Zustand. EIN-Transition nur
    wenn cur=false und alle EIN-Bedingungen erfüllt (debounced).
    AUS-Bedingungen dominieren immer.
  EIN-PV-Bedingung (D+/Gel): pv_ok = mppt_ok UND
    (PV(MA) >= Schwelle ODER chargeState == MPPT_CS_FLOAT).
    MPPT-Ausfall blockiert damit nur die EIN-Seite.
  AUS-Priorität (first-error-wins) für D+-Relais — alle HART,
    sofort ohne Debounce (B-11-Express-Route):
    1. BMS ungültig/veraltet
    2. Landstrom
    3. SoC < socDPlusOff
  AUS-Priorität für Gel-Lader — alle HART:
    1. BMS ungültig/veraltet
    2. Landstrom               (NEU v5.5)
    3. SoC < socGelOff
  AUS-Priorität für WR-Remote (kein Auto-EIN, s. L-SW05; gilt auch
    während Manuell-EIN und beendet diesen):
    1. BMS ungültig/veraltet   (hart)
    2. SoC < socWROff          (weich, debounced)
    HINWEIS: Die früher hier gelistete Landstrom-Bedingung war seit
    v5.3 aus dem Code entfernt (Renogy-NVS) — Doku-Drift in P-SW10,
    korrigiert in v5.5.
  v5.5 ENTFERNT: MPPT-Timeout als AUS-Bedingung (D+/Gel), weiche
    PV-AUS-Formeln (socDPlusHigh/pvThresholdOff/socGelHigh),
    D+-Mindestlaufzeit (LOGIC_DPLUS_MIN_ON_MS — hemmte nur weiche
    Gründe, die es nicht mehr gibt), WR-Auto-EIN (socWROn).
  Manual-Semantik (v5.5): siehe L-SW23. Persistentes Manuell-AUS in
    NVS "logic"/manOff0..2 — Restore in logic_init(), Schreiben in
    logic_set_manual() IMMER außerhalb des Spinlocks (Flash-Write
    unter taskENTER_CRITICAL unzulässig).
  PV-Glättung: MA über LOGIC_PPV_MA_WINDOW = 15 Samples (30s).
    Bei MPPT-Ausfall: 0 in Puffer schieben → gradueller Rückgang.
  MPPT-Recovery-Debounce: mppt_ok erst nach LOGIC_MPPT_RECOVERY_MIN
    = 5 aufeinanderfolgenden gültigen Frames (≈ 10s).
  Staleness-Timeout: BMS_STALE_TIMEOUT_MS = 60s (v4.x: 10s).
  Debounce-Zähler: db_dplus, db_gel, db_wr als int8_t, ±N Zyklen.
  Reason-Strings: char[64]-Puffer + portMUX Spinlock (kein Race).

P-SW11 | LOGGING-ARCHITEKTUR
  Puffer A — PSRAM Ringpuffer (2s-Rohdaten, 48h):
    ps_malloc(86400 × 32 Byte) = 2,76 MB PSRAM, zirkulär, s_mutex.
    Abruf: logger_snapshot(offset, count, step, out) — kurzer Lock.
  Puffer B — Minuten-Mittelung:
    Laufender Akkumulator, commit je Minute. Bis zu 60 Zeilen gepuffert.
    Batch-Flush alle logIntervalMs (Default 900s) oder bei vollem Puffer.
  SD-Zugriff: alle SD.open/close/read/write unter g_sdMutex.
    Notfallsicherung: Mutex mit 30ms-Timeout, then best-effort.
  HARDWARE-HINWEIS (ESP32-S3): SD.begin(CS) allein nutzt NICHT die
    konfigurierten Pins. Explizite Initialisierung erforderlich:
    static SPIClass sdSPI(FSPI); sdSPI.begin(CLK,MISO,MOSI,CS);
    SD.begin(CS, sdSPI) — Reihenfolge CLK/MISO/MOSI/CS beachten.
  CSV: Dateiname /log_NNNNN.csv (NNNNN = Tage seit 1970 in UTC).
    Zeitstempel ts-Spalte = UTC-Epoch; ts_mez (Anzeige) = lokal/DST.
  Notfallsicherung: logger_emergency_back_up() schreibt offene Minute
    + Puffer + Crash-Marker (/crash.log) + NVS-Uhrzeit.

P-SW12 | WEBSERVER-ARCHITEKTUR
  Framework:   ESPAsyncWebServer (non-blocking)
  HTTP Port:   80 (statische Dateien + REST)
  WebSocket:   Pfad /ws, Port 80.
  REST-Endpunkte:
    GET  /api/live           → aktueller Zustand (JSON)
    GET  /api/params         → aktuelle Parameter (JSON, 12 Felder)
    POST /api/params         → Parameter setzen (JSON-Body, max 2kB)
    POST /api/reset          → Fabrik-Reset
    POST /api/time           → Browser-Zeit-Sync {"epoch":...}
    GET  /api/tz             → Zeitzone {tz, abbr, offset}
    POST /api/tz             → POSIX-TZ setzen {"tz":"..."}
    GET  /api/wifi           → STA-Status + 3 Slots {slots:[{ssid,
                               set}],connected,ssid,ip,rssi} — ohne
                               Passwörter (v5.5.1; Endpunkt existierte
                               seit v5.1, fehlte hier in der Liste)
    POST /api/wifi           → Slot setzen {"slot":1..3,"ssid","pass"}
                               (ohne "slot" → Slot 1, abwärtskompat.;
                               leere SSID löscht den Slot; fehlendes
                               "pass" behält das gespeicherte)
    GET  /api/buffer         → Ringpuffer (?offset&count&step)
    GET  /api/sdfiles        → Liste CSV-Dateien auf SD
    GET  /api/sddata         → SD-Datei lesen (?file&skip&limit)
    GET  /api/level          → Lagesensor-Live-Zustand (JSON)
    GET  /api/levelcfg       → Lagesensor-Konfiguration (JSON)
    POST /api/levelcfg       → Lagesensor-Konfiguration setzen
    POST /api/levelcalib     → Lagesensor kalibrieren {"reset":bool}
    GET  /api/ota            → OTA-Info: Version, laufende/nächste
                               Partition, App-Max, LittleFS-Größen
    POST /api/ota?type=fw|fs → Web-OTA-Upload (Multipart): Firmware
                               (U_FLASH) bzw. Dashboard (U_SPIFFS),
                               siehe P-SW20
  WebSocket-Broadcast: alle 2s. ws.makeBuffer() + ref-counting.
    ws.cleanupClients() nur aus WS-Event-Handler (AsyncTCP-Thread).
  handle_buffer: logger_snapshot → PSRAM + AsyncResponseStream.
  handle_sddata: Pfad-Validierung, g_sdMutex, col >= 12,
    AsyncResponseStream.
  handle_sdfiles: unter g_sdMutex.
  handle_time: bool clock_set_epoch() ausgewertet.
  handle_tz_get/post: clock_tz()/clock_set_tz(), Längen-Plausibilität
    (2..63 Zeichen); ungültiger POSIX-String fällt in libc auf UTC.
  collect_body_chunk(): Multi-Chunk-POST-Akkumulation.
  HARDWARE-HINWEIS: server.on()-API-Routen MÜSSEN vor serveStatic("/")
    registriert werden — sonst Fallthrough zu LittleFS, API-Aufrufe
    laufen ins Leere (404 statt JSON).

P-SW13 | DASHBOARD (data/index.html)  [GEÄNDERT v5.4]
  Technologie: Reines HTML5/CSS3/JavaScript, keine externen Libraries.
  Tabs: Live | Verlauf | System | Lage | Einstellungen
  Kopf: Verbindungsstatus, Echtzeit-Uhr (lokale Zeitzone, DST), SD-Status,
        Puffergröße, Laufzeit. Footer zeigt aktive TZ-Abkürzung (z. B. CEST).
  Live-Tab:
    - SoC als SVG-Bogen mit Farbwechsel (grün/orange/rot)
    - BMS-Karte (V/I/P, Temperaturen, SoC, Kapazität, MOSFET-Status)
    - MPPT-Karte (PV-Leistung, Ladezustand, Tagesertrag, Max heute)
    - Live-Graph: 6 Metriken (SoC, Spannung, Strom, PV-Leistung,
      PV-Spannung, Rest-Ah), 5 Zeitfenster (30m/2h/6h/24h/48h),
      HH:MM-Achse in lokaler Zeitzone, Gradient-Fill
  Verlauf-Tab:
    - SD-Dateiliste via /api/sdfiles
    - History-Graph aus /api/sddata (bis 2.000 Punkte)
  System-Tab:
    - 4 Aktor-Kacheln (Grund, Debounce, socHigh-Info)
    - System-Status (Puffer, SD, Frame-/Fehlerzähler, Laufzeit)
    - Wechselrichter-Status-LED (Renogy NVS): dreistufig — aus (grau,
      Power-LED aus), grün (Power-LED an / "Ein"), rot (Alarm-LED an,
      Vorrang vor grün). Bei valid=false (vor erstem Poll / Mutex-
      Timeout) LED grau + Label "–". Quelle: inv.power_on / inv.alarm.
    - Firmware-Update-Karte (v5.4.1): Typ-Auswahl (Firmware/
      Dashboard), Dateiauswahl (.bin), Fortschrittsbalken via
      XHR-upload.onprogress, Sicherheitsabfrage (mit Warnung bei
      Dateiname↔Typ-Mismatch) vor dem Start, automatischer Reconnect
      nach dem Neustart (pollt /api/live, lädt die Seite neu). Beim
      Öffnen des Tabs wird GET /api/ota geladen (aktive Version +
      laufende Partition im Badge). Siehe P-SW20.
  Lage-Tab (optional, nur sinnvoll mit verbautem MMA8452Q):
    - Libellen-SVG: Blase wandert zur hohen Seite (Roll/Pitch-Vektor),
      grün bei "eben" (|Roll|,|Pitch| < Toleranz), sonst akku-farben
    - Top-Down-Fahrzeugansicht: Keilhöhe je Rad in cm, Referenzrad
      (höchstes, kein Keil) grün markiert, übrige amber
    - Kalibrierungs-Schaltflächen: aktuelle Lage als "eben" speichern
      bzw. Null-Offset zurücksetzen
    - Konfigurationsformular: Spurweite, Radstand, Einbaudrehung,
      Achsen-Invertierung, Modul aktiv/inaktiv
    - Polling 4Hz (250ms) nur während Tab aktiv ist, sonst inaktiv
      (kein unnötiger I2C-Traffic im Hintergrund)
  Einstellungen-Tab:
    - Formular für alle 10 NVS-Parameter
    - Speichern → POST /api/params, Fabrik-Reset → POST /api/reset
    - Zeitzonen-Auswahl (Dropdown gängiger EU-Zonen + benutzerdef.
      POSIX-String) → GET/POST /api/tz, zeigt aktive Abkürzung+Offset
  Zeit-Sync: POST /api/time bei Connect und alle 5 min.

P-SW14 | VARIABLE PARAMETER (NVS, Namespace "womo_cfg") [GEÄNDERT v5.5]
  10 Parameter (v5.5: 13→10):
  Key                   Default   Einheit  Grenzen       Gruppe
  ─────────────────────────────────────────────────────────────────
  socDPlusOn            95        %        50–100  }
  socDPlusOff           80        %        20–99   } D+-Relais
  pvDPlusMinW           100       W        10–2000 } (neu v5.5)
  ─────────────────────────────────────────────────────────────────
  socGelOn              95        %        50–100  }
  socGelOff             80        %        20–99   } Gel-Lader
  pvGelMinW             20        W        0–500   } (socGelOff neu)
  ─────────────────────────────────────────────────────────────────
  socWROff              80        %        20–99     Wechselrichter
  ─────────────────────────────────────────────────────────────────
  relayDebounceCycles   10        Zyklen   1–60
  logIntervalMs         900000    ms       60000–3600000
  manualTimeoutMin      30        min      1–240   (nur EIN D+/Gel)
  ─────────────────────────────────────────────────────────────────
  Plausibilitätsgrenzen werden geprüft (Wertebereich je Parameter).
  Keine erzwungene ON > OFF Cross-Validierung (entfällt ab v5.0).
  v5.5 ENTFERNT: socDPlusHigh, pvThresholdOn/Off (→ pvDPlusMinW),
  socGelHigh, socWROn. Migration in params_init(): neue Schlüssel
  mit Defaults anlegen, entfallene per prefs.remove() aufräumen.
  Bestehende Werte von socGelOn/pvGelMinW bleiben erhalten — nach
  dem Update im Dashboard prüfen (neue Defaults 95%/20W) oder
  Werks-Reset ausführen.

P-SW15 | SICHERHEITS-IMPLEMENTIERUNGEN  [GEÄNDERT]
  1.  Watchdog zweistufig: HW-WDT(4s) + SW-WDT (BMS/MPPT/Logger).
  2.  Fail-Safe GPIO: Sicher-Zustand VOR pinMode(OUTPUT).
  3.  Debounce: 10 Zyklen Mindeststabilität vor Relais-Änderung.
  4.  Mutex-Timeouts: nie blockierend > 200ms.
  5.  JSON-Größenbegrenzung: max. 2000 Einträge pro API-Anfrage.
  6.  Heap-Monitor: Warnung bei < 20kB freiem SRAM.
  7.  PSRAM static_assert: Compile-Fehler wenn LogEntry != 32 Byte.
  8.  SD-Datei sofort schließen nach Schreibvorgang.
  9.  Plausibilitätsgrenzen bei allen params_set_*() Funktionen.
  10. Zeitplausibilität: Epoch < 2024 wird verworfen (clock_set_epoch).
  11. BMS-Staleness: g_bms.valid + millis()-Differenz < 60s.
  12. MPPT-Staleness: valid + !timeout + millis()-Differenz.
  13. Pfad-Validierung in handle_sddata: "/" am Anfang, kein "..".
  14. POST-Body-Limit: > 2048 Byte → HTTP 413.

P-SW16 | BEKANNTE OFFENE PUNKTE (SOFTWARE)  [GEÄNDERT v5.4.1]
  ERLEDIGT SEIT v5.1:
   • RTC: DS3231 an I2C GPIO 1/2 bestückt und produktiv — führende
     Zeitquelle, übersteht Stromausfälle eigenständig (siehe L-SW19).
   • Lagesensor: MMA8452Q verbaut, level.h/.cpp + Dashboard-Tab +
     REST-API vollständig implementiert (v5.4, siehe L-SW21/P-SW19).
   • Web-OTA: Firmware- und Dashboard-Update per Browser vollständig
     implementiert (v5.4.1, ota.h/.cpp, Dual-App-Partitionslayout,
     siehe L-SW22/P-SW20). Vollständiger Build verifiziert (Firmware
     ~998kB / 24% der 4MB-App-Partition, littlefs.bin passt in die
     8000kB-spiffs-Partition, Magic-Byte-Schutz geprüft). OFFEN:
     einmalige USB-Migration auf das Dual-App-Layout am realen Gerät
     durchführen (Ablauf siehe P-SW20).

  OFFEN:
  1. JK-BMS-Verkabelung: 3-Pin-JST-GH-Stecker (CAN/RS485-Port) nicht
     beschaffbar — umgestellt auf direkte UART-TTL-Verdrahtung am
     GPS-Port (4-Pin, vorhandener Steckertyp), KEIN MAX485 mehr.
     Pin 4 (VBAT, volle Pack-Spannung) bleibt unbeschaltet. Pinpegel
     am Gerät verifiziert: Pin 2/3 ~2,5V (Standard-Pinbelegung,
     keine Spiegelung bei diesem Exemplar). BMS-Menü weiterhin auf
     Protokoll "RS485 Modbus" stellen (gilt unabhängig vom Port).
     OFFEN: erste Kommunikationsverifikation. Community-Berichte
     deuten an, dass JK-Firmware ab ca. V11.28 über den GPS-Port
     teils nicht mehr antwortet — unser Gerät (FW V11.287H) liegt
     genau in diesem Bereich. Bei Stille: TX/RX (Pin 2/3) tauschen,
     dann ggf. Fallback auf bmscan.cpp (CAN) oder MAX485-Doppel-
     brücke (TTL→RS485→TTL, beide Module bereits vorhanden).
  2. WR-Status (Renogy NVS): 2 LED-Eingänge implementiert (v5.3),
     GPIO 4/5 über Spannungsteiler. Pinbelegung am Fernbedienungs-
     port und Spannungsteiler-Dimensionierung noch nicht am realen
     Gerät verifiziert — Multimeter-Messung steht aus (Platzhalter-
     werte in config.h, s. Anschluss_Anleitung MODUL 8b). AC-Watt/
     Volt-Telemetrie bleibt grundsätzlich unmöglich (kein Protokoll
     am Gerät vorhanden, anders als ursprünglich bei Edecoa erhofft).
  3. BMS-Byte-Offsets: Protokoll-Doku verifiziert (v5.0).
     Strom-Skalierung: 0.01A/Bit per Doku ✓
     0x79-Triplets: 1 Byte Index + 2 Bytes mV per Doku ✓
     Temp-Kodierung: 0-140, >100 negativ per Doku ✓
     0xAA Nennkapazität: Einheit Ah direkt per Doku ✓ (Doku: "4 HEX AH")
     Strom 0x84: JK FW V11.287H → int16 Zweierkomplement, +=Laden ✓
     Alle Protokoll-Parameter verifiziert und abgeschlossen.
  4. VE.Direct HEX TX: in v5.0 implementiert (Register 0x2003,
     sn16 °C×100, 10s-Intervall, N/A-Fallback bei BMS-Timeout).
     Erste Hardware-Verifikation der MPPT-Reaktion ausstehend.
  5. WebSocket Thread-Safety: vollständige Lösung erfordert FreeRTOS-
     Queue aus der ein Callback im AsyncTCP-Thread liest.
  6. Delphi FireMonkey App (Phase 2): BLE 5.0 oder REST/WebSocket-API,
     Android + Windows, Read-Only + Reset.
  7. Remote-Telemetrie (Phase 2, geplant, noch keine Entscheidung):
     SIM7080G (UART-angebunden, TinyGSM + MQTT über TLS) vs.
     Walter-Board (ESP32-S3 + Sequans GM02SP, TLS/MQTT im Modem
     selbst). Serverseitige Dashboard-Architektur würde die PSRAM-
     Pufferanforderung am Gerät reduzieren. IoT-SIM-Kandidaten:
     1NCE (Einmalgebühr, 500MB/10 Jahre, 170+ Länder), melita.io
     (Pay-per-Use), Telekom Smart Connect S.
  8. Lagesensor (MMA8452Q, v5.4): Hardware verbaut, Firmware/REST-API
     vollständig, Backend-Lücke aus dem Branch-Merge nachgetragen
     (siehe Änderungsprotokoll v5.3→v5.4). OFFEN: Funktionstest am
     Fahrzeug — Kalibrierung in der Ebene durchführen, Keilwerte
     gegen real untergelegte Keile plausibilisieren, Einbaudrehung
     (c_rot) ggf. an tatsächliche Sensor-Orientierung anpassen.

P-SW17 | JK-BMS CAN-BUS-ALTERNATIVE (bmscan.h/.cpp)  [NEU v5.1]
  Zweck: Alternative Anbindung zum JK-BMS über CAN statt RS485, falls
  CAN bevorzugt oder RS485-Wiring nicht praktikabel ist. 100% aufruf-
  und antwortkompatibel zu bms.h — identische Struct BMSData,
  identische globale Symbole g_bms/g_bmsMutex, identische Funktionen
  bms_init()/bms_poll()/bms_to_json(), identischer JSON-Output
  (gleiche Keys/Formate) — austauschbar ohne Änderung an logic.cpp,
  http_server.cpp oder dem Dashboard.
  Quelle: JIKONG "BMS-CAN communication protocol" V1.02 (20190428).
  Protokoll: CAN 2.0A, Standard-Frame (11-Bit), 250 kbit/s,
  Little-Endian. BMS sendet zyklisch (Push) — kein Polling/Query,
  im Gegensatz zur RS485-Variante.
  Treiber: ESP-IDF TWAI (native CAN), LISTEN_ONLY-Modus, RX-Queue-
  Tiefe 64 (BATT_ST-Frame alle ~20ms, Queue wird je Poll-Zyklus
  komplett geleert, neueste Werte behalten).
  Frame-Matching: auf FUNC-Nibble der 11-Bit-ID (ID = FUNC<<8 | SA),
  NICHT auf volle ID — Workaround für einen JK-Firmware-Bug, bei dem
  sich die CAN-ID nach Adresswechsel nicht zuverlässig aktualisiert.
  Stromkonvention: CAN-Doku definiert + = ENTLADEN / − = LADEN
  (invers zur RS485-Variante) — wird intern invertiert, damit
  logic.cpp unverändert für beide Varianten funktioniert.
  Temperatur-Mapping (CAN liefert Max/Min/Avg statt 3 Einzelsensoren):
  tempMOS←MaxCellTemp, tempSensor1←MinCellTemp, tempSensor2←AvgCellTemp
  (AvgCellTemp ist der Wert, der per HEX-TX an den MPPT geht).
  Nicht im CAN-Protokoll enthalten → fest 0/false: nominalCapacityAh,
  remainingCapacityAh, chargeMOSFETEnabled, dischargeMOSFETEnabled.
  Auswahl (Compile-Zeit, beide Implementierungen bleiben im Repo):
    Standard-Build → bms.cpp aktiv (bmscan.cpp inaktiv via #ifdef
                     BMS_USE_CAN, keine Symbol-Kollision).
    CAN-Build      → platformio.ini: build_flags = -D BMS_USE_CAN,
                     build_src_filter = +<*> -<bms.cpp>
                     (bms.cpp aus dem Build nehmen, sonst doppelte
                     Symbole für g_bms/bms_init/etc.)
  Pin-Wiederverwendung: CAN-Transceiver (z. B. SN65HVD230, 3,3V) an
  denselben Pins wie die UART-TTL-Direktverbindung (GPIO 16/17),
  Override in config.h möglich.

P-SW18 | RGB-LED RUNDLAUF-ENCODING (io.h/.cpp, logic.cpp)  [NEU v5.1]
  Ersetzt das bisherige einfache Status-Schema (v5.0: GPIO 48 nur als
  "Status"-LED ohne dokumentierte Prioritätslogik) durch ein
  Rundlauf-Encoding: jeder Sensor/Aktor bekommt einen eigenen,
  zyklisch wiederkehrenden Slot fester Länge, statt dass ein Zustand
  einen anderen verdrängt. Slot-POSITION = Kanal-Identität — so ist
  erkennbar, WELCHER Kanal aktiv ist, unabhängig davon wie viele
  andere Kanäle gleichzeitig aktiv sind.
  Slot-Reihenfolge (fix, led_task() in io.cpp):
    0 BMS-Fehler | 1 MPPT-Fehler | 2 SoC-Gauge (immer sichtbar,
    farbcodiert: rot < RGB_SOC_GAUGE_CRIT_PCT, orange < LOW_PCT,
    gelb < MID_PCT, sonst grün) | 3 Landstrom | 4 D+ Kühlschrank |
    5 Gel-Lader | 6 WR-Remote.
  Datenfluss: g_rgbChannelMask (32-Bit, Bits 0–5 = Flags BMS/MPPT/
  Landstrom/D+/Gel/WR, Bits 8–15 = SoC%) wird atomar in EINEM
  Schreibvorgang von logic_evaluate() gesetzt und vom LED-Render-Task
  (Core 0, eigene FreeRTOS-Task) gelesen — vermeidet Races durch
  Single-Word-Read/Write ohne Mutex-Overhead für 7 Einzelwerte.
  Sonderfall Heap-Alert: g_rgbHeapAlert (von loop() bei Heap-Mangel
  gesetzt) hat im LED-Task Vorrang vor dem Rundlauf — magenta, sofort,
  ohne auf einen Zyklus-Slot zu warten. Bewusste Ausnahme: kein
  Betriebszustand, sondern ein Selbstschutz-Signal vor drohendem
  Heap-Crash.
  Bootzustand: RGB_BOOT (weiß) bis logic_evaluate() erstmals läuft,
  danach RGB_RUNNING (Rundlauf aktiv).
  Ansteuerung: neopixelWrite() (Arduino-Core 2.x, RMT-basiert, keine
  externe Library), global gedimmt über RGB_LED_BRIGHTNESS (0–255,
  Nachtsicht-Tauglichkeit im Womo), nur aus dem LED-Render-Task
  angesprochen (Single-Thread, RMT-sicher).
  Status auch im REST/WS-JSON sichtbar (io_to_json(): rgb-Objekt mit
  bms_fault, mppt_fault, soc, landstrom, d_plus, gel_lader, wr_remote).

P-SW19 | LAGESENSOR (level.h/.cpp)  [NEU v5.4]
  Sensor: MMA8452Q, 3-Achs-Beschleunigungssensor, I2C-Adresse 0x1D
  (SA0=1, Default-Breakout) oder 0x1C (SA0=0) — beide werden beim
  Init durchprobiert. WHO_AM_I-Register (0x0D) muss 0x2A liefern,
  sonst gilt der Sensor als nicht erkannt (present=false).
  Setup: STANDBY → XYZ_CFG=0x00 (±2g, kein High-Pass-Filter, max.
  Auflösung) → CTRL2=0x02 (High-Resolution-Oversampling) →
  CTRL1=0x21 (50Hz Datenrate + ACTIVE, 12-bit).
  Rohwerte: 12-bit links-justiert, ±2g → 1024 LSB/g.
  Glättung: EMA (α=0,30) über alle drei Achsen gegen Vibration/
  Erschütterung, bevor Roll/Pitch berechnet werden.
  Neigungsberechnung: roll = atan2(ay,az), pitch = atan2(-ax,
  sqrt(ay²+az²)) — Standardformeln für Schwerkraftvektor-basierte
  Neigung, in Grad.
  Einbaudrehung (c_rot, 0/90/180/270°): rotiert roll/pitch um die
  Hochachse, falls der Sensor nicht achsenparallel zum Fahrzeug
  verbaut werden konnte. Zusätzlich optionale Achsen-Invertierung
  (invRoll/invPitch) für spiegelverkehrten Einbau.
  Überkopf-Einbau (c_flipZ, NEU v5.5): Sensor kopfüber montiert
  (az ≈ −1g) → Roll läuft auf ±180°. Diese Orientierung kann WEDER
  durch c_rot (Hochachse) noch durch inv* abgebildet und darf NICHT
  per Null-Kalibrierung "gelöst" werden: atan2 springt bei ±180° um
  360° → tan(roll) explodiert, Keilwerte werden unbrauchbar. flipZ
  negiert ay/az vor der Neigungsberechnung (Spiegelung um die
  Längsachse) — Roll-/Pitch-Vorzeichen bleiben korrekt; andere
  Überkopf-Orientierungen ergeben sich kombiniert mit c_rot/inv*.
  Beim Umschalten wird die EMA neu initialisiert (f_init=false).
  Kalibrierung (Null-Offset): aktueller Roh-Roll/Pitch wird als
  "eben" in NVS gespeichert (zRoll/zPitch) und von allen folgenden
  Messungen abgezogen. Reset löscht den Offset (zurück auf 0/0).
  Ohne gültige Messung (present/valid=false) wird die Kalibrierung
  abgelehnt — keine sinnvolle Referenz vorhanden.
  Keilgeometrie: z_i = x_i·tan(roll) + y_i·tan(pitch) [mm], mit
  x=±Spurweite/2, y=±Radstand/2 je Rad (Räder: VL(-x,+y) VR(+x,+y)
  HL(-x,-y) HR(+x,-y)). Höchstes z_i wird Referenz (Keil 0); für
  alle anderen Räder gilt keil_i = max(z) − z_i (negative Werte auf
  0 geklemmt). level_to_json() liefert wedge[] in mm; das Dashboard
  zeigt die Anzeige in cm (Division durch 10 im Frontend).
  "Eben"-Schwelle: |roll| UND |pitch| < LEVEL_TOLERANCE_DEG (1,2°).
  (Doku-Drift korrigiert v5.5: hier stand 0,3°, config.h führte seit
  jeher 1,2f — config.h ist der gelebte Stand.)
  Bus-Koordination: alle I2C-Transaktionen (Erkennung, Setup, Poll)
  laufen unter g_i2cMutex (siehe P-SW06) — DS3231 und Lagesensor
  teilen sich GPIO 1/2, Zugriff aus unterschiedlichen Tasks/Cores.
  Threading: level_task (Core 0, ~4Hz/250ms) pollt den Sensor und
  legt das Ergebnis in einem per-portMUX geschützten Cache ab.
  Fehlertoleranz (NEU v5.5): valid kippt erst nach 4 aufeinander-
  folgenden Fehlversuchen (≈1s) auf false; bis dahin bleiben die
  letzten gültigen Werte stehen. Vorher setzte JEDER einzelne
  verpasste g_i2cMutex-Take (Kollision mit dem DS3231-Zugriff des
  Clock-Tasks, Timeout war 40ms) valid=false — die Dashboard-
  Libelle sprang dadurch regelmäßig kurz grau/mittig ("Sensor
  liest …"). Mutex-Timeout im Poll zusätzlich 40→100ms.
  HTTP-Handler und WebSocket-Broadcast lesen ausschließlich den
  Cache — kein I2C-Zugriff im Web-/AsyncTCP-Kontext.
  Hotplug-Verhalten: ist das Modul aktiviert aber kein Sensor
  erkannt, versucht level_task alle 3s erneut die Erkennung (z. B.
  für nachträglichen Einbau ohne Neustart).
  Persistenz: NVS-Namespace "level" (getrennt von "womo_cfg" und
  "clock") — Spurweite, Radstand, Rotation, Invertierung, flipZ
  (v5.5), Enabled, Null-Offset.
  Optional/ausfalltolerant: Modul ohne Einfluss auf Schaltlogik,
  RGB-LED oder andere Subsysteme. Fehlender oder defekter Sensor
  zeigt sich nur im eigenen Dashboard-Tab (present=false), sonst
  keine Auswirkung.
  REST-API: siehe P-SW12 (/api/level, /api/levelcfg, /api/levelcalib).
  Nachgetragener Merge-Fehler (siehe Änderungsprotokoll v5.3→v5.4):
  die HTTP-Handler fehlten zunächst nach dem Branch-Merge — level.cpp
  und der Lage-Tab waren vollständig, aber ohne Server-Anbindung
  (404 statt JSON). Beim Doku-Update nachgetragen und ergänzt.

P-SW20 | WEB-OTA (ota.h/.cpp)  [NEU v5.4.1]
  Zweck: Firmware und Dashboard ohne PC/USB über den Browser
  aktualisieren (L-SW22). Eigenes Modul, hält http_server.cpp schlank.
  Partitionslayout (partitions_16mb.csv, Dual-App):
    nvs 0x9000/0x5000 | otadata 0xE000/0x2000 |
    ota_0 0x10000/0x400000 | ota_1 0x410000/0x400000 |
    spiffs 0x810000/0x7D0000 (8000kB LittleFS) |
    coredump 0xFE0000/0x20000. Summe exakt 16MB, Apps 64k-aligned.
    LittleFS schrumpft ggü. Single-App von ~11,8MB auf 8000kB —
    unkritisch (dort liegt nur index.html).
  Update-Mechanik (Arduino <Update.h>):
    - type=fw → U_FLASH: schreibt in die inaktive App-Partition
      (esp_ota_get_next_update_partition), Update.h schaltet nach
      Update.end() die otadata-Boot-Auswahl um.
    - type=fs → U_SPIFFS: überschreibt die spiffs-Partition mit dem
      LittleFS-Image. LittleFS.end() VOR dem Schreiben (Partition
      wird komplett ersetzt); serveStatic liefert danach bis zum
      Neustart nichts mehr, die bereits geladene Seite läuft weiter.
    - Größe unbekannt (Multipart-Overhead) → Update.begin(
      UPDATE_SIZE_UNKNOWN, cmd); Update.end(true) finalisiert mit der
      tatsächlich geschriebenen Länge.
  Endpunkte:
    GET  /api/ota → ota_to_json(): version (FW_VERSION), partition
      (laufend), next (Ziel), app_max, fs_total, fs_used.
    POST /api/ota?type=fw|fs → Multipart-Upload. onUpload-Callback
      (ota_handle_upload) schreibt chunked im AsyncTCP-Task;
      onRequest-Callback (ota_handle_request) antwortet nach
      Abschluss und deckt auch den Leerer-Body-Fall ab (kein
      post_empty_guard nötig). Routen VOR serveStatic registriert
      (siehe P-SW12-Hinweis).
  Sicherheits-/Robustheitsmaßnahmen:
    - Magic-Byte-Prüfung: fw-Upload muss mit 0xE9 (ESP-Image-Magic)
      beginnen — verhindert das versehentliche Flashen von
      littlefs.bin als Firmware (führte sonst zu nicht bootender App).
    - Genau EIN Upload gleichzeitig: globaler Claim (s_inProgress +
      s_activeReq) unter portMUX-Spinlock (Test-and-Set); zweiter
      paralleler Upload wird abgewiesen.
    - Per-Request-Zustand (OtaReqState: claimed/success/cmd/written/
      err) liegt heap-alloziert in req->_tempObject. Der Request-
      Destruktor ruft free(_tempObject) (verifiziert esp32async-Fork
      v3.7.0, WebRequest.cpp) → kein Leak auch bei Client-Abbruch,
      keine Interferenz zwischen zeitgleichen Uploads, kein globaler
      Fehler-/Fortschritts-State.
    - Client-Abbruch: req->onDisconnect ruft Update.abort() und gibt
      den Claim frei — feuert auch nach ERFOLGREICHER Antwort
      (normales TCP-Close), daher nur abbrechen, wenn dieser Request
      den Claim noch hält.
    - Neustart deferred: ota_handle_request setzt nur s_rebootAtMs
      (volatile uint32_t, 32-bit-atomar auf Xtensa) auf millis()+
      1500ms; ota_tick() im loop-Kontext (Core 1) führt VOR dem
      ESP.restart() logger_emergency_back_up("OTA_REBOOT") aus. NIE
      Neustart im AsyncTCP-Handler (Response muss erst raus, und ein
      restart aus dem TCP-Callback ist unsauber).
    - Watchdogs unberührt: Upload läuft im async_tcp-Task, loop()
      tickt weiter (HW-WDT), die SW-WDT-Module (BMS/MPPT/Logger)
      laufen normal. Kein zusätzlicher esp_task_wdt-Eingriff nötig.
  Migration von <v5.4.1 (Single-App): einmalig USB nötig, da sich
    die Partitionsgeometrie ändert:
      1. Schaltparameter, Heim-WLAN (SSID), Zeitzone und Lage-
         Kalibrierung notieren (werden gelöscht).
      2. pio run -t erase  (löscht ALLE NVS-Namespaces: womo_cfg,
         wifi, clock, level).
      3. pio run -t upload  (neue Firmware + Dual-App-Tabelle).
      4. pio run -t uploadfs  (LittleFS in die neue spiffs-Partition).
      5. Parameter/WLAN/TZ/Kalibrierung im Dashboard neu setzen.
    Danach laufen alle weiteren Updates ausschließlich über den
    Browser. Erzeugung der Images: firmware.bin via `pio run`,
    littlefs.bin via `pio run -t buildfs` (jeweils unter
    .pio/build/esp32-s3-devkitc-1/).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ÄNDERUNGSPROTOKOLL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

─── v5.5 → v5.5.1 ───────────────────────────────────────────────

  MULTI-SSID HEIM-WLAN:
  • Bis zu 3 Heimnetze konfigurierbar (NVS "wifi": ssid1/pass1 …
    ssid3/pass3). Migration in webserver_init(): alter Einzel-Key
    "ssid"/"pass" wird beim ersten Boot nach Slot 1 gehoben und
    entfernt. [L-SW10]
  • Auswahlstrategie: >1 Netz → asynchroner WiFi-Scan, Verbindung
    zum STÄRKSTEN bekannten Netz; Auswertung in wifi_tick() im
    ws_task (gleiche Kontext-Regel wie das Deferred-Apply — keine
    WiFi-Calls im AsyncTCP-Handler). Solange getrennt: Rescan alle
    60s. Genau 1 Netz → Direktverbindung + AutoReconnect (Verhalten
    identisch zu v5.5). Keine WiFiMulti-Klasse (blockierendes run()
    inkompatibel mit AP+STA-Dualmodus). [L-SW10, P-SW12]
  • /api/wifi: GET liefert slots[]+verbundene SSID, POST nimmt
    {"slot":1..3,...}; ohne "slot" abwärtskompatibel Slot 1.
    Endpunkt dabei erstmals in die P-SW12-Endpunktliste aufgenommen
    (fehlte dort seit v5.1 — Doku-Lücke). [P-SW12]
  • Dashboard: WLAN-Karte mit 3 Slot-Zeilen (SSID+Passwort+
    Speichern je Slot); Statuszeile zeigt jetzt die verbundene
    SSID. live-JSON net{} um "ssid" erweitert.
  • DEFAULT_WIFI_STA_SSID/PASSWORD aus config.h entfernt (seit dem
    Slot-Modell ungenutzt; Slots defaulten auf leer).

─── v5.4.1 → v5.5 ───────────────────────────────────────────────

  D+-PEGEL-FIX (KRITISCH):
  • RELAY_D_PLUS_ACTIVE LOW→HIGH: Der in der Anschluss_Anleitung
    dokumentierte Hardware-Umbau auf das Joy-it COM-MOSFET (Active-
    HIGH) war in config.h nie nachgezogen worden. Folge im Feld:
    D+ EIN/AUS invertiert UND D+ ab Boot aktiv (io_init() setzte
    hartkodiert HIGH als "sicher" nach alter Active-LOW-Logik).
    io_init() leitet die Fail-Safe-Pegel jetzt für ALLE Ausgänge
    aus den *_ACTIVE-Makros ab (!ACTIVE), nie mehr hartkodiert.
    [P-SW15, Anschluss_Anleitung MODUL 5]

  SCHALTKRITERIEN-REDESIGN (D+/GEL/WR):
  • D+/Gel: nur noch HARTE AUS-Bedingungen (Landstrom, BMS ungültig/
    veraltet, SoC<Off). Weiches PV-AUS, socHigh-Konzept und D+-
    Mindestlaufzeit entfallen. [L-SW03, L-SW04, P-SW10]
  • MPPT-Ausfall ist KEINE AUS-Bedingung mehr — ein VE.Direct-
    Glitch bei vollem Akku schaltete bisher Kühlschrank und Gel-
    Lader sofort hart ab, obwohl energetisch nichts passiert war.
    mppt_ok bleibt EIN-Voraussetzung (PV/Float sonst nicht
    bewertbar). [L-SW03, L-SW04, P-SW10]
  • EIN-PV-Bedingung neu: PV(MA) >= Schwelle ODER MPPT-Ladezustand
    Float (CS=5) — im Float ist der Akku voll und Überschuss
    vorhanden. [L-SW03, L-SW04, P-SW10]
  • Gel: Landstrom neu als hartes AUS; eigener socGelOff-Parameter
    (bisher harte AUS-Schwelle = socGelOn). Defaults: socGelOn
    82→95%, pvGelMinW 60→20W. [L-SW04, P-SW14]
  • WR: KEIN Auto-EIN mehr — Einschalten nur manuell. socWROn
    entfällt. Auto-AUS (BMS hart, SoC<WROff weich) gilt auch
    während Manuell-EIN und beendet ihn. [L-SW05, L-SW23, P-SW10]
  • Parameterbereinigung 13→10: −socDPlusHigh −pvThresholdOn/Off
    −socGelHigh −socWROn, +pvDPlusMinW +socGelOff. NVS-Migration
    in params_init() (neue Keys anlegen, alte entfernen).
    [P-SW14, L-SW14]

  MANUAL-SEMANTIK (v5.5):
  • Deadman-Timeout gilt nur noch für Manuell-EIN von D+/Gel.
    WR-Manuell-EIN ohne Timer (Sicherheitsnetz = Auto-AUS-Bed.).
    Manuell-AUS dauerhaft: kein Timer, kein Rückfall auf Auto,
    NVS-persistent (Namespace "logic", manOff0..2, Restore in
    logic_init(), Flash-Write stets außerhalb des Spinlocks).
    Aufhebung nur durch expliziten Auto-/Ein-Befehl. Der v5.4-
    Override war zudem bisher nicht normativ dokumentiert —
    nachgeholt als L-SW23. [L-SW23, P-SW10]

  LAGESENSOR:
  • Überkopf-Einbau (flipZ): behebt Roll ≈ ±180° bei kopfüber
    montierter Platine. Negiert ay/az vor der Neigungsberechnung;
    NICHT per Null-Kalibrierung lösbar (atan2-Sprung bei ±180° →
    tan explodiert). Neue Checkbox im Lage-Tab, NVS-Key "flipZ",
    /api/levelcfg erweitert. [L-SW21, P-SW19]
  • Fehlertoleranz Libelle: valid erst nach 4 Fehlversuchen in
    Folge (≈1s) false — einzelne g_i2cMutex-Kollisionen mit dem
    Clock-Task ließen die Anzeige bisher bei 4Hz kurz grau/mittig
    springen. Mutex-Timeout 40→100ms. [P-SW19]

  DOKU-KORREKTUREN:
  • P-SW10: Landstrom stand noch als WR-AUS-Bedingung (Code seit
    v5.3 ohne) — Drift entfernt.
  • P-SW19: Eben-Schwelle 0,3°→1,2° (config.h-Wert ist der
    gelebte Stand).

─── v5.4 → v5.4.1 ───────────────────────────────────────────────

  WEB-OTA (NEU):
  • Firmware- und Dashboard-Update direkt aus dem Browser (System-
    Tab), ohne PC/USB. Neues Modul ota.h/.cpp: Upload-Handler
    (Update.h), Magic-Byte-Prüfung (0xE9) für Firmware, per-Request-
    Zustand in req->_tempObject, genau ein Upload gleichzeitig
    (Spinlock-Claim), Client-Abbruch via onDisconnect → Update.abort().
    [L-SW22, P-SW02, P-SW20]
  • Neustart deferred in loop() (ota_tick), NIE im AsyncTCP-Kontext;
    vor ESP.restart() logger_emergency_back_up("OTA_REBOOT").
    [P-SW05, P-SW20]
  • Partitionstabelle auf Dual-App-OTA umgestellt: nvs 20k | otadata
    8k | ota_0 4MB | ota_1 4MB | spiffs 8000k | coredump 128k
    (exakt 16MB, Apps 64k-aligned). LittleFS von ~11,8MB auf 8000k
    verkleinert (unkritisch). ERFORDERT einmalige USB-Migration mit
    pio run -t erase (löscht alle NVS-Namespaces). [P-SW01, P-SW20]
  • Zwei neue Endpunkte: GET /api/ota (Version/Partition/Größen),
    POST /api/ota?type=fw|fs (Multipart-Upload). [P-SW12, P-SW20]
  • Dashboard: neue Karte "Firmware-Update" im System-Tab mit Typ-
    Auswahl, Fortschrittsbalken (XHR), Sicherheitsabfrage und
    automatischem Reconnect nach Neustart. [P-SW13]
  • Zentrale Versionskonstante FW_VERSION in config.h eingeführt —
    speist Boot-Banner (main.cpp) und GET /api/ota. [P-SW02]

  KONSISTENZ (kein Funktionsbruch):
  • Versionsstrings aller Datei-Kopfzeilen, Boot-Banner, Dashboard-
    Titel/-Footer und Doku auf v5.4.1 vereinheitlicht.
  • Vollständiger Build verifiziert (pio run + pio run -t buildfs),
    Partitionstabelle rechnerisch und im generierten partitions.bin
    geprüft, Magic-Bytes beider Images bestätigt (firmware.bin=0xE9,
    littlefs.bin≠0xE9).

─── v5.3 → v5.4 ─────────────────────────────────────────────────

  LAGESENSOR (NEU):
  • Elektronische Wasserwaage über MMA8452Q am gemeinsamen I2C-Bus
    mit der DS3231-RTC (GPIO 1/2, kein neuer GPIO nötig). Neue Datei
    level.h/.cpp: Register-Treiber, EMA-Glättung, Roll/Pitch via
    atan2, Keilgeometrie (keil_i = max(z) − z_i), NVS-Persistenz
    Namespace "level", eigener Poll-Task (Core 0, 4Hz). [L-SW21,
    P-SW03, P-SW05, P-SW19]
  • Neuer g_i2cMutex (deklariert in clock.h) koordiniert DS3231 und
    Lagesensor auf dem geteilten Bus. [P-SW06]
  • Dashboard: neuer Tab "Lage" — Libellen-SVG, Top-Down-
    Fahrzeugansicht mit Keilhöhen je Rad (cm), Kalibrierungs-
    Buttons, Konfigurationsformular. 4Hz-Polling nur während der
    Tab aktiv ist. [L-SW21, P-SW13]
  • Vier neue REST-Endpunkte: GET /api/level, GET/POST /api/levelcfg,
    POST /api/levelcalib. [P-SW12, P-SW19]

  MERGE-NACHARBEIT (beim Zusammenführen von feature/lagesensor
  nach main aufgefallen, kein Designfehler im Feature selbst):
  • Die vier o.g. HTTP-Handler waren im gemergten Stand zunächst
    NICHT registriert (server.on() fehlte in http_server.cpp/.h) —
    level.cpp und das Dashboard-Frontend waren vollständig, der
    Server gab auf alle /api/level*-Aufrufe 404 zurück. Kompilierte
    und bootete sauber, der Lage-Tab blieb im Betrieb aber funktions-
    los. Nachträglich ergänzt nach dem Muster der bestehenden
    Handler (collect_body_chunk, StaticJsonDocument). [P-SW12, P-SW19]
  • clock_local_offset_at() griff nach dem Merge wieder direkt auf
    lt.tm_gmtoff zu (struct tm hat dieses Feld in der hier genutzten
    newlib-Variante NICHT) — Compile-Fehler. Portabel über mktime()
    gelöst: gmtime_r() zerlegt die UTC-Epoch, mktime() interpretiert
    die Felder als lokale Wandzeit (gemäß gesetztem POSIX-TZ) und
    rechnet zurück nach Epoch; die Differenz ist der gesuchte lokale
    Offset inkl. Sommerzeit (tm_isdst=-1 vor dem mktime()-Aufruf,
    damit DST anhand der TZ-Regel für das jeweilige Datum bestimmt
    wird, nicht angenommen). [L-SW19, P-SW07]
  • Ein verwaister, duplizierter Abschnitts-Kommentar
    "<!-- EINSTELLUNGEN -->" vor der neuen Lage-Section in
    data/index.html wurde entfernt (rein kosmetisch, kein
    Funktionsfehler).

─── v5.1 → v5.3 ─────────────────────────────────────────────────

  BMS-ANBINDUNG:
  • Der 3-Pin-JST-GH-Stecker für den CAN/RS485-Port des BMS war
    nicht beschaffbar. Umgestellt auf direkte UART-TTL-Verdrahtung
    am GPS-Port (4-Pin, vorhandener Steckertyp) — KEIN MAX485 mehr
    nötig. Pinpegel am Gerät verifiziert (Pin 2/3 ~2,5V, kein VBAT).
    Bekanntes Restrisiko: JK-Firmware ab ca. V11.28 antwortet bei
    manchen Geräten über den GPS-Port nicht mehr zuverlässig — unser
    Gerät (FW V11.287H) liegt in diesem Bereich, erste Kommuni-
    kationsverifikation steht noch aus. Fallback bei Stille:
    bmscan.cpp (CAN) oder MAX485-Doppelbrücke. [L-SW01, P-SW03,
    P-SW04, P-SW16]

  WECHSELRICHTER:
  • Hardwarewechsel: Edecoa (RJ12-Protokoll-Sniffer, nie über den
    Stub hinaus implementiert) ersetzt durch Renogy 12V/2000W mit
    eingebauter Netzvorrangschaltung (NVS). Kein Datenprotokoll am
    Fernbedienungsport — stattdessen 2 debounced LED-Status-
    Eingänge (Power/Alarm) über Spannungsteiler an GPIO 4/5.
    AC-Watt/Volt-Telemetrie ist mit dieser Hardware grundsätzlich
    nicht verfügbar. inverter.cpp ist kein Stub mehr. [L-SW17,
    P-SW09, P-SW16]
  • L-SW05: Landstrom-Bedingung aus der WR-Remote-Schaltlogik
    entfernt — das Renogy-NVS übernimmt die AC-seitige Umschaltung
    Landstrom/Wechselrichter eigenständig, ein hartes Abschalten
    der Fernsteuerung durch die Firmware ist bei diesem Gerät nicht
    mehr nötig. [L-SW05, L-SW06]
  • Dashboard System-Tab: dreistufige Wechselrichter-Status-LED
    (grau=ungültig, grün=ein, rot=Alarm).

  LANDSTROM-ERKENNUNG:
  • Hardwarewechsel von PC817-Optokoppler (MeanWell HDR-15-5) auf
    Spannungsteiler (2kΩ/1,5kΩ an 5V-Rail) am selben GPIO 18.
    Logik unverändert (HIGH = Landstrom), pinMode INPUT ohne
    Pull-up (kein PULLUP wie zuvor dokumentiert). [L-SW06, P-SW03]

─── v5.0 → v5.1 ─────────────────────────────────────────────────

  ZEITBASIS:
  • L-SW19: DS3231-RTC bestückt und produktiv — führende Zeitquelle
    statt reiner Browser-Sync. Fallback-Kette DS3231→NVS→Build-Zeit.
    Health-JSON {present,valid,drift,temp} (clock_rtc_json()).
    Hardware-Fix: delay(100) + 3×-Retry nach Wire.begin() (I2C-Timeout
    beim Kaltstart, Error 263). [L-SW19, P-SW03, P-SW07, P-SW16]

  BMS-ANBINDUNG:
  • Neu: bmscan.h/.cpp — CAN-Bus-Alternative zu bms.cpp/RS485, 100%
    kompatibler JSON-Output, Compile-Zeit-Auswahl via BMS_USE_CAN.
    [L-SW01, P-SW02, P-SW17]

  STATUS-ANZEIGE:
  • RGB-LED von einfachem Status-Schema auf Rundlauf-Encoding
    umgestellt: alle 7 Kanäle (BMS/MPPT/SoC/Landstrom/D+/Gel/WR)
    gleichberechtigt in festen Slots statt Prioritätsverdrängung.
    Heap-Alert bleibt Sonderfall (sofort, magenta). Neuer Parameter
    g_rgbChannelMask (Bitmask) ersetzt g_rgbState (Enum) als primären
    Zustand. [L-SW20, P-SW03, P-SW18]

  HARDWARE-INBETRIEBNAHME (Erkenntnisse, kein API-Bruch):
  • N16R8-Boot-Loop: board_upload.flash_size UND board_build.flash_size
    müssen beide 16MB sein, sonst verwirft der Bootloader die
    Partitionstabelle (Endlos-Reset ohne App-Ausgabe). [P-SW01]
  • SD.begin(CS) nutzt auf dem ESP32-S3 nicht automatisch die
    konfigurierten SPI-Pins — explizite SPIClass(FSPI) nötig. [P-SW11]
  • Webserver: API-Routen müssen vor serveStatic("/") registriert
    werden, sonst Fallthrough zu LittleFS (404 statt JSON). [P-SW12]

─── v4.3 → v5.0 ─────────────────────────────────────────────────

  DATENERFASSUNG:
  • L-SW01: BMS-Datenumfang reduziert (Zellspannungen, Zyklenanzahl,
    Balancingstatus, Alarm-/Schutz-Bitmasks entfernt — bewusst).
    [L-SW01, P-SW08, P-SW13]
  • L-SW02: MPPT-Felder reduziert (VPV, LOAD/IL, H19, H22/H23, HSDS,
    PID, FW, SER# entfernt); VE.Direct HEX TX für BMS-Kerntemperatur
    hinzugefügt (P-SW09, P-SW04). [L-SW02, P-SW04, P-SW09]

  SCHALTLOGIK:
  • L-SW03/P-SW10: Staleness-Timeout BMS+MPPT von 10s auf 60s erhöht.
    Neue AUS-Formel D+: (PV<pvThreshOff UND SoC<socDPlusHigh) ODER
    SoC<socDPlusOff — PV allein löst kein AUS mehr aus (erst in
    Kombination mit niedrigem SoC). Neuer Parameter: socDPlusHigh.
  • L-SW04/P-SW10: pvGelMinW 30W→60W; neue AUS-Formel Gel:
    (PV<pvGelMinW UND SoC<socGelHigh) ODER SoC<socGelOn.
    socGelOff entfernt (hard AUS = socGelOn = 82%).
    Neuer Parameter: socGelHigh (Default 90%).
  • L-SW05: PV-Schwellwerte für WR entfernt — rein SoC-basiert.
    pvWRThresholdOn/Off entfernt. [P-SW14]

  ZEITBASIS:
  • L-SW19: Zeitzone als wählbarer POSIX-TZ-String mit automatischer
    Sommerzeit (DST), Default Europe/Berlin (MEZ/MESZ). Vorher feste
    MEZ (UTC+1) ohne DST. Storage/Rechnung intern UTC; Anzeige + lokale
    CSV-Spalte über localtime_r umgerechnet. SD-Dateinamen jetzt nach
    UTC-Tag (vorher MEZ-Tag). Neu: GET/POST /api/tz, NVS "clock"/"tz".
    config.h: CLOCK_MEZ_OFFSET_SEC entfernt → DEFAULT_TZ.
    Uhr-Zurückdrehen durch Browser erlaubt (kein Monotonizitätsschutz).

  HARDWARE/GPIO:
  • P-SW03: GPIO 42 → UART2 TX (MPPT VE.Direct HEX, neu).
    GPIO 1 (SDA) + GPIO 2 (SCL) → DS3231 I2C (reserviert).
    GPIO 3 als Strapping nicht mehr fälschlich in Reserve.

  KONFIGURATION:
  • P-SW14: 12 statt 13 Parameter. +socDPlusHigh +socGelHigh
    −socGelOff −pvWRThresholdOn −pvWRThresholdOff, pvGelMinW 30→60W.
  • L-SW14/P-SW15: Cross-Validierung (ON > OFF-Pflicht) entfernt.

  DASHBOARD:
  • P-SW13/L-SW12: Alarm-Box, Zellspannungsbalken, Schutzparameter,
    Balancer-Anzeige aus Dashboard entfernt (Daten nicht mehr gelesen).
    Metrikliste: 7→6 (Zell-Δ entfernt).

─── v4.2 → v4.3 ─────────────────────────────────────────────────
  • Hysterese wiederhergestellt, BMS-Staleness, !mppt_ok AUS-Bedingung,
    WR-eigene PV-Parameter, PV-Glättung (MA 30s), MPPT-Recovery-Debounce,
    D+-Mindestlaufzeit 5min, Cross-Core-Race Reason-Strings gefixt,
    g_sdMutex, logger_snapshot, AsyncResponseStream, col>=12,
    clock_set_epoch bool, 64-Bit-Zeitbasis, platformio.ini gepinnt.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ERSTELLTE QUELLDATEIEN (Stand v5.4.1)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ platformio.ini            (gepinnte Abhängigkeiten; optional
                               BMS_USE_CAN für CAN-Build, siehe P-SW17)
  ✓ partitions_16mb.csv       (GEÄNDERT v5.4.1: Dual-App-OTA-Layout —
                               otadata + ota_0/ota_1 statt factory,
                               spiffs auf 8000k, siehe P-SW01/P-SW20)
  ✓ src/ota.h      ✓ src/ota.cpp      (NEU v5.4.1: Web-OTA, Firmware-
                               & Dashboard-Upload per Browser, Update.h,
                               Magic-Byte-Schutz, deferred Reboot,
                               siehe L-SW22/P-SW20)
  ✓ src/config.h              (v5.4.1: FW_VERSION-Konstante;
                               v5.3: GPIO 1/2 produktiv (DS3231),
                               RGB-Rundlauf-Konstanten, DEFAULT_TZ statt
                               CLOCK_MEZ_OFFSET_SEC, GPIO 4/5 Renogy
                               WR-Status (GPIO_WR_LED_POWER/ALARM,
                               WR_LED_ACTIVE), Landstrom-Sensor als
                               Spannungsteiler statt Optokoppler;
                               v5.4: LEVEL_I2C_ADDR_PRIMARY/ALT,
                               LEVEL_POLL_INTERVAL_MS, LEVEL_TOLERANCE_DEG)
  ✓ src/params.h   ✓ src/params.cpp   (GEÄNDERT v5.0: 12 Parameter,
                               socGelOff entfernt, *High neu, pvWR entfernt)
  ✓ src/bms.h      ✓ src/bms.cpp      (GEÄNDERT v5.0: reduziertes JSON;
                               UART-TTL direkt am GPS-Port, kein MAX485,
                               DE/RE-Toggle GPIO 15 No-Op, siehe P-SW04)
  ✓ src/bmscan.h   ✓ src/bmscan.cpp   (NEU v5.1: CAN-Alternative zu
                               bms.cpp, identischer JSON-Output, siehe P-SW17)
  ✓ src/mppt.h     ✓ src/mppt.cpp     (GEÄNDERT v5.0: HEX-TX Temperatur,
                               GPIO 42 TX, reduzierte Felder)
  ✓ src/inverter.h ✓ src/inverter.cpp (GEÄNDERT v5.3: kein Stub mehr —
                               Renogy NVS, 2 debounced LED-Status-Eingänge
                               GPIO 4/5, g_invMutex, siehe L-SW17/P-SW09)
  ✓ src/level.h    ✓ src/level.cpp    (NEU v5.4: Lagesensor MMA8452Q,
                               EMA-Glättung, Roll/Pitch via atan2,
                               Keilgeometrie, NVS "level", level_task
                               4Hz Core 0, siehe L-SW21/P-SW19)
  ✓ src/io.h       ✓ src/io.cpp       (GEÄNDERT v5.1: RGB-Rundlauf-
                               Encoding statt Prioritäts-Enum, siehe P-SW18;
                               v5.3: Landstrom-Sensor Spannungsteiler statt
                               Optokoppler, GPIO 18 unverändert)
  ✓ src/logic.h    ✓ src/logic.cpp    (GEÄNDERT v5.1: setzt
                               g_rgbChannelMask statt g_rgbState; v5.3:
                               WR-Remote-Logik ohne Landstrom-Bedingung,
                               siehe L-SW05)
  ✓ src/logger.h   ✓ src/logger.cpp   (GEÄNDERT v5.1: SD-Dateiname nach
                               UTC-Tag; SD-SPI-Hinweis ESP32-S3 siehe P-SW11)
  ✓ src/clock.h    ✓ src/clock.cpp    (GEÄNDERT v5.1: DS3231 produktiv,
                               führende Zeitquelle; wählbarer POSIX-TZ +
                               DST via localtime_r, /api/tz, NVS "clock"/"tz";
                               v5.4: g_i2cMutex für geteilten Bus mit
                               Lagesensor; clock_local_offset_at() auf
                               portablen mktime()-Offset umgestellt
                               (tm_gmtoff nicht exponiert, siehe P-SW07))
  ✓ src/watchdog.h ✓ src/watchdog.cpp (unverändert)
  ✓ src/http_server.h ✓ src/http_server.cpp  (GEÄNDERT v5.1: /api/tz
                               (GET/POST), tz-Abk. im Live-JSON, ts_mez
                               DST-korrekt; Routenreihenfolge siehe P-SW12;
                               v5.3: Wechselrichter-Status-LED im System-Tab;
                               v5.4: vier Lagesensor-Routen /api/level*
                               nachgetragen, siehe P-SW12/P-SW19;
                               v5.4.1: GET/POST /api/ota registriert,
                               ota.h eingebunden, siehe P-SW20)
  ✓ src/main.cpp              (v5.4: level_init()/level_task eingebunden;
                               v5.4.1: ota_init() nach webserver_init(),
                               ota_tick() in loop(), Boot-Banner nutzt
                               FW_VERSION)
  ✓ data/index.html           (GEÄNDERT v5.1: Zeitzonen-Auswahl im
                               Einstellungen-Tab, dynamisches MEZ/MESZ-Label;
                               Dashboard ohne Zellspannungen, 6 Metriken;
                               v5.3: dreistufige WR-Status-LED im System-Tab;
                               v5.4: neuer Tab "Lage" — Libelle, Keile,
                               Kalibrierung, Konfigurationsformular;
                               v5.4.1: Firmware-Update-Karte im System-Tab
                               (Web-OTA, Fortschritt, Reconnect), siehe P-SW13)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
